{
  "eli3": [
    "Word2Vec (Mikolov et al. 2013) learns dense vector representations (embeddings) for words such that semantically similar words have similar vectors: vector('king') − vector('man') + vector('woman') ≈ vector('queen'). The famous analogy test. The key insight: words appearing in similar contexts have similar meanings — learn representations by predicting context from word (Skip-gram) or word from context (CBOW).",
    "There are two architectures: Skip-gram predicts surrounding context words given the center word; CBOW predicts the center word from surrounding context words. Both are trained with negative sampling: instead of computing a softmax over all V words, sample k negative (non-context) words and train a binary classifier. This reduces training cost from O(V) to O(k) per word — critical for vocabularies of 100K+.",
    "Word2Vec produces static embeddings: 'bank' has the same embedding regardless of context (river bank vs bank account). This was a major limitation, addressed by contextual embeddings (ELMo, BERT). However, Word2Vec embeddings remain useful for: fast inference, low-resource scenarios, and as initialization for neural models. Pre-trained embeddings (GoogleNews 300d, CommonCrawl) are available."
  ],
  "whenWhy": [
    {
      "title": "Semantic similarity and word clustering",
      "body": "Word2Vec embeddings capture semantic and syntactic regularities: king−man+woman≈queen (semantic analogy), fast−faster≈slow−slower (comparative pattern), Paris−France+Germany≈Berlin (country-capital). Cosine similarity between vectors measures semantic relatedness: cosine(cat, dog) > cosine(cat, table). Used for: synonym detection, word sense disambiguation (using nearest neighbors), semantic search (query expansion with similar words)."
    },
    {
      "title": "Initialization for downstream NLP models",
      "body": "Word2Vec embeddings serve as pre-trained initialization for neural NLP models, replacing random initialization. Pre-training on large corpora (100B words) captures general semantic knowledge — fine-tuning on task-specific data then adapts these representations. This was the standard approach before BERT: embed tokens with Word2Vec/GloVe, encode with LSTM, classify. Transfer learning from pre-trained word embeddings provides significant gains on small datasets."
    },
    {
      "title": "Fast inference and low-resource NLP",
      "body": "Word2Vec produces 100–300 dimensional static embeddings that are extremely fast at inference (just a lookup table). For production systems with strict latency budgets or IoT/edge devices, Word2Vec + simple classifiers outperform transformers. For low-resource languages, Word2Vec can be trained on small corpora (10M words) and still provides useful representations."
    }
  ],
  "deepDiveIntro": [
    "Skip-gram trains by maximizing the probability of observing the actual context words given the center word. Computing a full softmax over the entire vocabulary is expensive, so negative sampling approximates it by training a binary classifier that distinguishes true context words from randomly sampled noise words. Frequent words are sampled as negatives more often but with a dampening factor to avoid over-representing them. Each word maintains two vectors: one for when it is the center word and one for when it is a context word. [1]",
    "CBOW predicts the center word from the average of the surrounding context word vectors — it is faster because it makes fewer gradient updates per word. Skip-gram predicts each context word separately from the center word, making more updates per word and performing better on rare words. Skip-gram with negative sampling is the standard in practice and produces better-quality embeddings. [1]"
  ],
  "deepDive": [
    {
      "heading": "Skip-gram with Negative Sampling (SGNS)",
      "body": "For each true center-context word pair, the model maximizes the dot product between their vectors (making the prediction confident) while minimizing the dot product with randomly sampled noise words (pushing them apart). Typical settings use 5 to 20 negative samples, embeddings of 100 to 300 dimensions, and a context window of 5 to 10 words. Training cost scales with the number of negatives and the embedding dimension, not the full vocabulary size. [1]",
      "formula": "L_SGNS = log σ(vw · uc) + Σₖ log σ(−vw · unₖ)"
    },
    {
      "heading": "Linear Structure and Analogical Reasoning",
      "body": "Word embedding offsets encode semantic relationships: the vector from 'man' to 'king' is approximately the same as the vector from 'woman' to 'queen'. This linear structure emerges because Skip-gram with negative sampling implicitly factorizes a shifted version of the pointwise mutual information matrix between words and contexts. Analogy arithmetic — subtracting one relationship vector and adding another — works because these relationship directions are consistent across the embedding space. [1]",
      "formula": "king − man + woman ≈ queen"
    },
    {
      "heading": "Subsampling and Rare Word Handling",
      "body": "Very frequent words such as 'the' and 'is' are downsampled during training with a probability that increases with their frequency. This discards occurrences that contribute little information while retaining semantic content. Words appearing fewer than 5 times are removed entirely because they lack sufficient context to learn meaningful representations. Out-of-vocabulary words receive no embedding — the main weakness of Word2Vec compared to subword models like FastText. [1]"
    },
    {
      "heading": "Static vs Contextual Embeddings",
      "body": "Word2Vec assigns one vector per word form — identical for all usages. 'bank' has the same vector regardless of 'river bank' vs 'bank account'. This was fixed by contextual embeddings: ELMo (Peters et al. 2018) uses BiLSTM representations that change with context; BERT uses self-attention. For tasks sensitive to word sense disambiguation (WSD), coreference resolution, and complex semantic reasoning, contextual embeddings are necessary. Word2Vec remains useful as a fast, lightweight alternative when compute is constrained. [2]"
    }
  ],
  "youtubeLabel": "Word2Vec — StatQuest",
  "youtubeUrl": "https://www.youtube.com/embed/viZrOnJclY0",
  "takeaways": [
    "Skip-gram: predict context from center word; CBOW: predict center from context. SGNS is the standard. [1]",
    "Negative sampling: train binary classifier vs k random negatives — reduces cost from O(V) to O(k). [1]",
    "SGNS ≡ implicit factorization of shifted PMI matrix — explains linear analogy structure. [1]",
    "Limitation: static embeddings — 'bank' has one vector regardless of context (river vs financial). [2]",
    "FastText extends Word2Vec with character n-grams, handling OOV words by composing subword vectors. [1]"
  ],
  "edgeCases": [
    {
      "q": "Why does Word2Vec use two separate embedding matrices (center vs context)?",
      "a": "Each word w has a center vector vw (used when w is the center word) and a context vector uw (used when w is a context word). These capture different roles. After training, the center vectors are typically used as word representations; context vectors are discarded. Using a single shared matrix would enforce vw = uw, which is mathematically possible but empirically gives worse results — the asymmetry between center and context roles helps the optimization landscape. Some implementations add vw + uw as the final representation. [1]"
    },
    {
      "q": "How do you evaluate word embeddings beyond analogy tasks?",
      "a": "Analogy evaluation (Google analogies, MSR analogies) has been criticized for being too sensitive to the specific arithmetic used. Alternative evaluations: (1) Word similarity benchmarks (SimLex-999, WordSim-353) — cosine similarity should correlate with human judgments; (2) Downstream task performance (NER, sentiment, parsing accuracy when embeddings are used as features); (3) Intrinsic linguistic probing (syntactic probes, morphological probes). The field has moved toward evaluating embeddings via downstream task performance rather than intrinsic similarity benchmarks. [1]"
    }
  ],
  "sources": [
    { "label": "Mikolov et al. (2013) — Distributed Representations of Words and Phrases and their Compositionality, NeurIPS 2013", "url": "https://arxiv.org/abs/1310.4546" },
    { "label": "Peters et al. (2018) — Deep Contextualized Word Representations (ELMo), NAACL 2018", "url": "https://aclanthology.org/N18-1202/" }
  ]
}
