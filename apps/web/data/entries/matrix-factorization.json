{
  "visualizationType": "chart",
  "codeSnapshot": {
    "label": "matrix_factorization.py",
    "code": "import numpy as np\nfrom numpy.linalg import solve\n\n# ── 1. SVD-based Matrix Factorization (explicit ratings) ──\n# R: user-item rating matrix (m users × n items), NaN = unrated\nm, n, k = 5, 4, 2   # k = latent factors\nR = np.array([\n    [5, 3, np.nan, 1],\n    [4, np.nan, 4, 1],\n    [1, 1, np.nan, 5],\n    [np.nan, np.nan, 5, 4],\n    [np.nan, np.nan, 4, 4],\n])\n\nnp.random.seed(42)\nP = np.random.normal(0, 0.1, (m, k))   # user factor matrix\nQ = np.random.normal(0, 0.1, (n, k))   # item factor matrix\n\n# ── 2. SGD for matrix factorization (Funk SVD) ──\nlr = 0.01\nreg = 0.02\nfor epoch in range(100):\n    for u in range(m):\n        for i in range(n):\n            if not np.isnan(R[u, i]):\n                e_ui = R[u, i] - P[u] @ Q[i]\n                P[u] += lr * (e_ui * Q[i] - reg * P[u])\n                Q[i] += lr * (e_ui * P[u] - reg * Q[i])\n\nR_hat = P @ Q.T\nprint('Predicted ratings:')\nprint(R_hat.round(2))\n\n# ── 3. ALS — alternating closed-form updates ──\n# Fix Q, solve for P; fix P, solve for Q\ndef als_step(R, fixed, reg, k):\n    n_users = R.shape[0]\n    result = np.zeros((n_users, k))\n    for u in range(n_users):\n        rated = ~np.isnan(R[u])\n        Q_rated = fixed[rated]\n        r_u = R[u, rated]\n        # Closed form: P_u = (Q_ratedᵀQ_rated + λI)⁻¹ Q_ratedᵀ r_u\n        result[u] = solve(Q_rated.T @ Q_rated + reg * np.eye(k), Q_rated.T @ r_u)\n    return result\n\nfor _ in range(20):\n    P = als_step(R, Q, reg=0.1, k=k)\n    Q = als_step(R.T, P, reg=0.1, k=k)\n\nprint('ALS predictions:', (P @ Q.T).round(2))\n\n# ── 4. BPR (Bayesian Personalized Ranking) — implicit feedback ──\n# Maximises: P(r_{u,i} > r_{u,j}) for observed positive i, unobserved j\n# Uses SGD on triplets (u, i, j): u=user, i=positive, j=negative sample\n# Loss: -Σ ln σ(x_{ui} − x_{uj}) + λ‖θ‖²\n# where x_{ui} = p_u · q_i  (dot product score)\n# BPR is the standard for implicit feedback (purchase/click data)\n# Use the 'cornac' or 'surprise' library for production BPR"
  },
  "eli3": [
    "Matrix Factorization decomposes the huge user-item rating matrix into two smaller matrices: a user matrix and an item matrix, each with k latent 'hidden' dimensions. User row × item column gives the predicted rating. The k dimensions capture abstract concepts — for movies, one dimension might represent 'action-ness', another 'drama-ness' — but they're learned automatically, not hand-designed.",
    "Think of it as a compression algorithm. Instead of storing millions of (user, item, rating) triplets, you store m×k numbers for users and n×k numbers for items (much smaller). The product P·Qᵀ approximates the full rating matrix, and the latent factors capture the underlying structure that explains why users rate certain items similarly.",
    "Two key training algorithms: SGD (Stochastic Gradient Descent — update P and Q simultaneously using one observed rating at a time) and ALS (Alternating Least Squares — fix one matrix and solve for the other in closed form, alternating). For implicit feedback (no explicit ratings, just clicks/purchases), BPR (Bayesian Personalized Ranking) is used — it learns to rank observed items above unobserved ones."
  ],
  "whenWhy": [
    {
      "title": "Sparse rating matrices at scale",
      "body": "In a system with 1 million users and 100,000 items, the full rating matrix has 10¹¹ entries — but a typical user rates only 50–200 items, leaving 99.98% unobserved. Memory-based CF must operate on this sparse matrix directly. Matrix factorization compresses the data into user and item factor matrices (1M × 50 + 100k × 50 = 55 million parameters), enabling full-matrix imputation and recommendations in O(k) per user-item pair at inference time."
    },
    {
      "title": "Implicit feedback recommendation (no ratings available)",
      "body": "Most real-world interaction data is implicit: purchases, clicks, views, play duration. There are no explicit 1–5 ratings. BPR (Bayesian Personalized Ranking) and ALS with confidence weights (Hu et al., 2008) are the standard matrix factorization approaches for implicit data. They model preferences as a ranking problem — observed interactions are preferred over unobserved ones — rather than a regression problem. Spotify's music recommendation and Amazon's purchase prediction are historically built on implicit MF."
    },
    {
      "title": "Latent factor discovery",
      "body": "The k latent dimensions learned by matrix factorization often correspond to interpretable concepts — genre clusters in movies, style clusters in fashion, topic clusters in documents — without being explicitly defined. This makes MF a tool for exploratory analysis as well as recommendation. Principal Component Analysis (PCA) is a special case of SVD with orthogonality constraints; NMF (Non-negative Matrix Factorization) adds non-negativity constraints that tend to produce more interpretable 'parts-based' decompositions."
    }
  ],
  "deepDiveIntro": [
    "The user-item rating matrix is approximated by the product of a user factor matrix and an item factor matrix. The objective minimizes prediction error on observed ratings plus a regularization penalty on the magnitudes of both matrices. Adding user and item bias terms — absorbing the tendency of some users to rate harshly and some items to attract higher ratings overall — substantially improves prediction accuracy. [1]",
    "SGD updates one observed rating at a time: compute the error, then nudge both the user and item factor vectors slightly toward each other. ALS instead fixes one matrix and solves for the other in closed form, then alternates. ALS is particularly well suited to implicit feedback because the dense system it creates can be solved in parallel for each user independently. [2]"
  ],
  "deepDive": [
    {
      "heading": "Funk SVD and the Netflix Prize",
      "body": "Simon Funk popularized matrix factorization during the Netflix Prize by using SGD on observed ratings only, adding user and item bias terms to absorb systematic rating tendencies. The predicted rating for a user-item pair is the global average plus a user offset, an item offset, and the dot product of the user and item latent vectors.",
      "formula": "r̂_{u,i} = μ + b_u + b_i + pᵤᵀqᵢ   [with global mean and user/item biases]"
    },
    {
      "heading": "BPR: Bayesian Personalized Ranking for Implicit Feedback",
      "body": "Bayesian Personalized Ranking trains on triples of a user, an item they interacted with, and a randomly sampled item they did not interact with. The model learns to score the interacted item higher than the unobserved one. This directly optimizes ranking quality rather than prediction error, which is more appropriate when the goal is to recommend a list of items.",
      "formula": "BPR objective: max Σ_{(u,i,j)} ln σ(x̂_{uij}) − λ‖Θ‖²,   x̂_{uij} = r̂_{u,i} − r̂_{u,j}"
    },
    {
      "heading": "Evaluation: RMSE vs Ranking Metrics",
      "body": "This was the Netflix Prize metric — but RMSE does not directly measure recommendation quality. A model that perfectly predicts that a user will rate 3.0 vs 3.1 doesn't help ranking. Better metrics for production recommendation: NDCG@K (Normalised Discounted Cumulative Gain — gives more credit for relevant items at the top of the list), Precision@K, Recall@K, Hit Rate@K. BPR-optimized models typically outperform RMSE-optimised models on NDCG@10 by 5–15%, even with higher RMSE. Choose your training objective to match your evaluation metric."
    }
  ],
  "youtubeLabel": "Matrix Factorization for Recommender Systems — Standford CS246",
  "youtubeUrl": "https://www.youtube.com/embed/ZspR5PZemcs",
  "takeaways": [
    "R ≈ PQᵀ: user factors P (m×k) × item factors Q (n×k). Predicted rating r̂_{u,i} = μ + b_u + b_i + pᵤᵀqᵢ. [1]",
    "SGD (Funk SVD): update P,Q on observed ratings. ALS: alternating closed-form updates, preferred for implicit data. [2]",
    "BPR: for implicit feedback. Optimises ranking directly via triplets (u, positive item, negative item). [2]",
    "k (latent factors): typically 50–200. More factors → better fit, slower training, risk of overfitting. [1]",
    "Evaluation: RMSE for explicit ratings, NDCG@K / Precision@K for recommendation quality. [1]"
  ],
  "edgeCases": [
    {
      "q": "What is the difference between SVD, Funk SVD, and truncated SVD in this context?",
      "a": "True SVD: R = UΣVᵀ, exact decomposition, requires complete matrix (no missing values). Computing SVD on a sparse matrix with NaN entries requires imputation (fill with 0 or mean), then SVD — but imputed values dominate and bias the result. Truncated SVD (TruncatedSVD in sklearn): computes only the top k singular values, but still requires a complete matrix. Funk SVD: directly minimizes MSE on observed ratings only via SGD — no imputation needed, the loss only runs over Ω (observed pairs). Funk SVD is not mathematically SVD but produces a factorization R ≈ PQᵀ optimised for the observed ratings. In practice, 'matrix factorization for recommender systems' almost always means Funk-style SGD or ALS, not true SVD."
    },
    {
      "q": "How do you choose k (number of latent factors) in practice?",
      "a": "k controls the capacity of the model. Too small k: underfits — can't capture complex user preferences. Too large k: overfits — memorizes training ratings, poor generalisation to unobserved entries. Typical range: k = 20–200 for most recommendation problems. Tuning: (1) Grid search over k ∈ {20, 50, 100, 200}, evaluate on a held-out validation set using NDCG@10 or RMSE. (2) Regularisation strength λ interacts with k — larger k needs stronger regularisation. Always tune (k, λ) jointly. Rule of thumb: k=50 with λ=0.01–0.1 is a strong starting point for most datasets. Large-scale systems (Spotify, Netflix) use k=100–512 and tune per A/B experiment rather than offline evaluation."
    }
  ],
  "sources": [
    {
      "label": "Koren, Y., Bell, R. & Volinsky, C. — Matrix Factorization Techniques for Recommender Systems, IEEE Computer 42(8), 2009",
      "url": "https://doi.org/10.1109/MC.2009.263"
    },
    {
      "label": "Rendle, S. et al. — BPR: Bayesian Personalized Ranking from Implicit Feedback, UAI 2009",
      "url": "https://arxiv.org/abs/1205.2618"
    }
  ]
}
