{
  "eli3": [
    "Scaling laws describe how language model performance improves predictably with more parameters (N), more training data (D), and more compute (C). Kaplan et al. (2020) at OpenAI found that test loss follows a power law: L ∝ N^{-α} when holding D fixed, and L ∝ D^{-β} when holding N fixed. These are smooth, predictable relationships — doubling parameters reliably improves performance by a fixed amount.",
    "The key finding: for a fixed compute budget C, there is an optimal allocation between model size and data size. Kaplan et al. initially recommended scaling model size faster than data. Hoffmann et al. (2022) (Chinchilla) revised this: for a given compute budget, the optimal model is smaller but trained on more tokens than Kaplan suggested — roughly, N_opt ∝ C^{0.5} and D_opt ∝ C^{0.5} (equal scaling). GPT-3 (175B, 300B tokens) was undertrained according to Chinchilla.",
    "Scaling laws enable planning: before training a large model, predict its final loss from smaller runs. This derisk expensive training runs. Emergent abilities (Wei et al. 2022) — capabilities that appear suddenly at certain scale thresholds — are a related but debated phenomenon. Scaling laws have driven the LLM race: GPT-4, Claude, Gemini are all scaled according to Chinchilla-optimal or beyond principles."
  ],
  "whenWhy": [
    {
      "title": "Allocating compute budget for LLM training",
      "body": "Before training a large model, use scaling laws to find the compute-optimal allocation. Chinchilla law: for C compute FLOPs, optimal model has N* ≈ (C/6)^{0.5} parameters, trained on D* ≈ (C/6)^{0.5} tokens (6 FLOPs per token for forward+backward per parameter). Example: 10²³ FLOPs → 70B parameters, 1.4T tokens. This analysis showed GPT-3 (175B, 300B tokens) used too many parameters for too few tokens. Llama 1 (Touvron et al. 2023) followed Chinchilla — smaller model, more data."
    },
    {
      "title": "Predicting performance before training",
      "body": "Scaling laws allow extrapolation: train small models (100M, 1B, 10B) at different compute budgets, fit the power law, extrapolate to the target scale. This gives an expected loss for the large model before committing the compute. Used by OpenAI, DeepMind, Anthropic to make training decisions. The smooth power-law relationship means small-scale experiments reliably predict large-scale outcomes — reducing risk in expensive training runs."
    },
    {
      "title": "Understanding model efficiency trade-offs",
      "body": "Scaling laws reveal that for inference-heavy deployments (many queries per day), a smaller, better-trained model (Chinchilla-optimal) is more efficient than a large undertrained model. A 70B model trained on 1.4T tokens (Llama 2) outperforms a 175B model trained on 300B tokens (GPT-3) at similar compute, while being much cheaper to run. This insight drove the shift toward smaller, more data-efficient models in industry."
    }
  ],
  "deepDiveIntro": [
    "Kaplan et al. (2020) showed that language model test loss follows smooth power laws in model size, dataset size, and compute — each with a characteristic exponent holding over six or more orders of magnitude. Their key recommendation was to scale model size faster than data for a fixed compute budget. This was revised by Hoffmann et al. (Chinchilla, 2022) using more carefully controlled experiments.",
    "The Chinchilla scaling law models test loss as an irreducible entropy floor plus terms that decrease as power laws with model size and data size. Minimizing this loss subject to a fixed compute budget — which is approximately proportional to the product of model size and training tokens — yields the result that both should scale equally. The practical rule of thumb is about 20 training tokens per model parameter."
  ],
  "deepDive": [
    {
      "heading": "Power Law Relationships",
      "body": "Doubling model size reduces test loss by about 5 percent; doubling training data reduces it by a similar but slightly larger amount. These power laws hold consistently from tens of millions to tens of billions of parameters, and the relationship fits well on a log-log scale. The irreducible entropy floor represents the minimum achievable loss — noise inherent in the training data that no model can predict. Beyond that floor, more compute always helps. [1]",
      "formula": "L(N, D) = E + A/N^α + B/D^β"
    },
    {
      "heading": "Chinchilla Optimal Compute Allocation",
      "body": "Training compute is approximately proportional to model size times number of training tokens. Minimizing the Chinchilla loss function subject to a fixed compute budget shows that model size and training tokens should scale at the same rate — both proportional to the square root of the compute budget. The practical rule of thumb is 20 training tokens per parameter. GPT-3 used about 1.7 tokens per parameter, which by this standard is severely undertrained. [2]"
    },
    {
      "heading": "Emergent Abilities at Scale",
      "body": "Some capabilities — multi-step arithmetic, chain-of-thought reasoning, few-shot word unscrambling — appear absent below around 10 billion parameters and then appear sharply above some threshold. One explanation is that these tasks require multiple correct steps in sequence, and the probability of a complete correct chain only becomes meaningful once per-step accuracy is high enough. However, Schaeffer et al. (2023) argue that emergence may be a metric artifact: switching from discrete accuracy to continuous evaluation metrics makes the improvement smooth rather than sudden. [3]"
    },
    {
      "heading": "Scaling Beyond Chinchilla: Inference-Optimal Models",
      "body": "Chinchilla optimizes for a single training run. For inference-heavy deployments (LLM API serving millions of queries), the total compute = training + N_queries × inference_cost. Smaller models are cheaper per inference. Llama 2 70B: trained on 2T tokens (beyond Chinchilla optimal for 70B) — accepts higher training cost for lower inference cost. Llama 3 8B: trained on 15T tokens — extremely inference-efficient. 'Overtrained' small models (more data than Chinchilla-optimal) are increasingly preferred for deployment. [2]"
    }
  ],
  "takeaways": [
    "L(N,D) = E + A/N^α + B/D^β — power law over model size and data, holds 6+ orders of magnitude. [1]",
    "Kaplan: scale N faster than D. Chinchilla (2022): N and D scale equally — 20 tokens per parameter. [2]",
    "GPT-3 (175B, 300B tokens) was severely undertrained; Chinchilla (70B, 1.4T) matches at 4× less compute. [2]",
    "Emergent abilities: some capabilities appear sharply at scale thresholds — possibly metric artifact. [3]",
    "Inference-optimal: 'overtrain' smaller models for cheap deployment (Llama 3 8B on 15T tokens). [2]"
  ],
  "edgeCases": [
    {
      "q": "Do scaling laws hold for all model architectures and domains?",
      "a": "Kaplan and Chinchilla scaling laws were derived for autoregressive transformer LMs on natural language. They appear to generalize: similar power laws observed for vision transformers (Zhai et al. 2022, ViT scaling), code LMs (GitHub Copilot), and multimodal models. However, the exponents (α, β) and constants differ by domain and architecture. Mixture-of-Experts (MoE) models change the scaling dynamics — total parameters (N) vs active parameters per token are different. Sparse MoE scales differently than dense models. In practice, organizations refit scaling laws for their specific architecture, data mixture, and hardware configuration. [1][2]"
    },
    {
      "q": "What is the 'flop-matched' vs 'parameter-matched' comparison?",
      "a": "Two models are parameter-matched if they have the same N (parameters). Two models are FLOP-matched if they use the same training compute C = 6ND. A parameter-matched comparison is unfair if one model was trained on 10× more data — it had 10× more FLOPs. A FLOP-matched comparison is the fair way to compare architectures and training recipes. Chinchilla showed that Gopher (280B, 300B tokens) vs Chinchilla (70B, 1.4T tokens) are approximately FLOP-matched, and Chinchilla wins — same compute, better allocation. Always check whether LLM comparisons control for training compute, not just parameter count. [2]"
    }
  ],
  "sources": [
    { "label": "Kaplan et al. (2020) — Scaling Laws for Neural Language Models, arXiv 2001.08361", "url": "https://arxiv.org/abs/2001.08361" },
    { "label": "Hoffmann et al. (2022) — Training Compute-Optimal Large Language Models (Chinchilla), NeurIPS 2022", "url": "https://arxiv.org/abs/2203.15556" },
    { "label": "Wei et al. (2022) — Emergent Abilities of Large Language Models, TMLR 2022", "url": "https://arxiv.org/abs/2206.07682" }
  ]
}
