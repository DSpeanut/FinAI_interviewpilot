{
  "eli3": [
    "MCMC (Markov Chain Monte Carlo) is a family of algorithms for drawing samples from a probability distribution — specifically the posterior P(θ|x) — when direct sampling is impossible. Instead of sampling the posterior directly, MCMC constructs a Markov chain whose stationary distribution is the target posterior, then runs the chain long enough to collect samples from it.",
    "Metropolis-Hastings is the foundational MCMC algorithm: at each step, propose a new state θ* from a proposal distribution q(θ*|θ_current). Accept it with probability min(1, [P(θ*|x)q(θ_current|θ*)] / [P(θ_current|x)q(θ*|θ_current)]). The normalizing constant P(x) cancels in the ratio — this is why MCMC only needs P(θ|x) up to proportionality.",
    "Practical MCMC requires: (1) burn-in — discard early samples before the chain converges to stationarity; (2) thinning — keep every k-th sample to reduce autocorrelation; (3) diagnostics — check convergence with R̂ (Gelman-Rubin statistic, should be ≈ 1.0), effective sample size (ESS), and trace plots. Modern samplers (HMC, NUTS) are far more efficient than Metropolis for continuous parameters."
  ],
  "whenWhy": [
    {
      "title": "Computing intractable posteriors",
      "body": "The posterior P(θ|x) = P(x|θ)P(θ)/P(x) requires computing P(x) = ∫P(x|θ)P(θ)dθ, which is analytically intractable for most non-conjugate models. MCMC bypasses this by sampling from P(θ|x) ∝ P(x|θ)P(θ) without computing the normalizing constant. This enables Bayesian inference for hierarchical models, generative models, and any complex probabilistic model."
    },
    {
      "title": "Bayesian hierarchical models",
      "body": "Hierarchical models with many latent variables (group-level parameters, random effects) have joint posteriors that cannot be computed analytically. MCMC samples from the full joint posterior P(θ₁,…,θₖ,φ|x), enabling inference on all parameters simultaneously. Stan (No-U-Turn Sampler) and PyMC3 are standard tools for MCMC in hierarchical models."
    },
    {
      "title": "When variational inference is insufficient",
      "body": "Variational Bayes (VB) approximates the posterior with a tractable family and systematically underestimates posterior variance. MCMC provides asymptotically exact samples without distributional assumptions. Use MCMC when accurate uncertainty quantification is critical (clinical decisions, risk assessment) or when the posterior is multimodal — VB may miss modes entirely."
    }
  ],
  "deepDiveIntro": [
    "MCMC algorithms work by constructing a Markov chain whose long-run behavior matches the target distribution. A sufficient condition is detailed balance: the probability of transitioning from state A to state B weighted by the probability of being at A equals the reverse. The ergodic theorem then guarantees that running-average functions of the chain converge to their expectations under the target as the chain length grows. The acceptance ratio in Metropolis-Hastings is designed to enforce detailed balance with respect to the posterior.",
    "How quickly the chain converges to its stationary distribution depends on the shape of the posterior and the quality of the proposal. Random-walk Metropolis mixes slowly in high dimensions and when parameters are strongly correlated. Gibbs sampling sidesteps proposal tuning by drawing each parameter from its exact full conditional, but mixes poorly when parameters are correlated. Hamiltonian Monte Carlo uses gradient information to make large directed moves across the posterior surface."
  ],
  "deepDive": [
    {
      "heading": "Metropolis-Hastings Algorithm",
      "body": "Metropolis-Hastings builds a chain by proposing moves and then accepting or rejecting them according to how plausible the new state is under the target distribution. The key trick is that the difficult normalizing constant drops out of the acceptance ratio. This makes the method widely applicable, though it can mix slowly in high-dimensional or strongly correlated problems.",
      "formula": "α = min(1,  P(θ*|x)·q(θ_t|θ*) / [P(θ_t|x)·q(θ*|θ_t)])"
    },
    {
      "heading": "Gibbs Sampling",
      "body": "Gibbs sampling updates one block of parameters at a time using their full conditional distributions. Because each step is drawn exactly from the corresponding conditional, proposals are always accepted. The method is especially attractive in conjugate models, although it can move inefficiently when parameters are strongly correlated."
    },
    {
      "heading": "Hamiltonian Monte Carlo (HMC) and NUTS",
      "body": "Hamiltonian Monte Carlo uses gradient information to move through parameter space in long, directed trajectories rather than short random walks. This makes it dramatically more efficient for smooth continuous posteriors. The No-U-Turn Sampler automates the hardest tuning choice, which is why it has become the default engine in modern Bayesian software such as Stan.",
      "formula": "H(θ, p) = U(θ) + K(p) = −log P(θ|x) + p'M⁻¹p/2"
    },
    {
      "heading": "Convergence Diagnostics",
      "body": "Running a chain is not enough; you also need evidence that it explored the target distribution well. Diagnostics such as the split-chain convergence statistic, effective sample size, and trace plots help detect non-convergence and poor mixing. These tools do not prove correctness, but they are essential for deciding whether posterior summaries are trustworthy.",
      "formula": "ESS = T / (1 + 2Σρₖ)"
    }
  ],
  "youtubeLabel": "Markov Chain Monte Carlo — StatQuest",
  "youtubeUrl": "https://www.youtube.com/embed/yCv2N7wGDCw",
  "takeaways": [
    "MCMC samples from P(θ|x) ∝ P(x|θ)P(θ) without computing the normalizing constant P(x). [1]",
    "Metropolis-Hastings: propose θ*, accept with α = min(1, [π(θ*)q(θ|θ*)]/[π(θ)q(θ*|θ)]). [1]",
    "Gibbs sampling: requires closed-form full conditionals; acceptance rate = 1; mixes poorly for correlated parameters. [1]",
    "HMC/NUTS: uses gradient of log-posterior for efficient proposals; 10–1000× more efficient than random walk. [2]",
    "Diagnose convergence with R̂ ≈ 1.0 (Gelman-Rubin) and high ESS; discard burn-in samples. [1]"
  ],
  "edgeCases": [
    {
      "q": "Why does MCMC still work if the proposal is rejected many times?",
      "a": "Rejection keeps the chain at the current state θ⁽ᵗ⁾, which is still a valid sample from the stationary distribution. The algorithm is still valid — rejections just mean the effective sample size is lower (higher autocorrelation). Very low acceptance rates (e.g., < 5%) indicate the proposal variance is too large (proposals are in the tails of the posterior). Very high acceptance rates (> 80%) indicate the step size is too small (poor exploration). The optimal trade-off for random walk MH in d dimensions is ≈ 23.4% acceptance (Roberts et al., 1997). [1]"
    },
    {
      "q": "Can MCMC get stuck in local modes?",
      "a": "Yes. For multimodal posteriors, standard MCMC chains can get trapped in one mode for many iterations, producing samples that don't represent the full posterior. Solutions: (1) parallel tempering — run chains at different 'temperatures' and swap states; (2) simulated annealing — gradually lower temperature during sampling; (3) SMC (sequential Monte Carlo) — reweight and resample particles. HMC is especially prone to getting stuck at posterior modes with sharp boundaries. [1]"
    }
  ],
  "sources": [
    { "label": "Gelman et al. — Bayesian Data Analysis (3rd ed.), Ch. 11–12", "url": "http://www.stat.columbia.edu/~gelman/book/" },
    { "label": "Hoffman & Gelman (2014) — The No-U-Turn Sampler, JMLR 15(1)", "url": "https://jmlr.org/papers/v15/hoffman14a.html" }
  ]
}
