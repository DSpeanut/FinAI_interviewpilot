{
  "eli3": [
    "MAP (Maximum A Posteriori) estimation finds the mode of the posterior distribution: θ̂_MAP = argmax P(θ|x) = argmax [log P(x|θ) + log P(θ)]. It's MLE plus a regularization term from the log-prior. With a flat prior, MAP reduces to MLE exactly.",
    "With a Gaussian prior N(0,τ²) on each coefficient, log P(θ) = −||θ||²/(2τ²) + const. Adding this to the log-likelihood gives: θ̂_MAP = argmax [ℓ(θ) − λ||θ||²], where λ = 1/(2τ²). This is exactly Ridge regression. With Laplace priors, MAP gives Lasso. Thus, regularized ML estimation is Bayesian MAP with specific priors.",
    "MAP is a point estimate — it uses only the mode of the posterior, discarding all information about posterior shape. It's faster than full Bayesian inference but misses uncertainty. For symmetric unimodal posteriors, MAP equals the posterior mean; for skewed posteriors, they diverge. A MAP estimate with no uncertainty quantification can be overconfident."
  ],
  "whenWhy": [
    {
      "title": "Regularized regression: MAP = penalized MLE",
      "body": "Ridge regression (L2): Gaussian prior N(0,1/λ) → MAP adds −λΣθᵢ² to log-likelihood. Lasso (L1): Laplace prior → MAP adds −λΣ|θᵢ|. Elastic net: mixture of Gaussian and Laplace priors → MAP adds −λ₁Σ|θᵢ| − λ₂Σθᵢ². This equivalence provides a Bayesian interpretation of regularization: the penalty term is −log P(θ), and λ controls prior strength (small λ = weak prior, large λ = strong shrinkage)."
    },
    {
      "title": "Computationally efficient posterior approximation",
      "body": "When full posterior computation (MCMC) is expensive, MAP provides a fast point estimate. The Laplace approximation builds on MAP: after finding θ̂_MAP, approximate the posterior as Gaussian centered at θ̂_MAP with covariance [−H]⁻¹ (inverse negative Hessian). This enables uncertainty estimation without full MCMC. Used in neural networks (Laplace-approximated Bayesian NNs)."
    },
    {
      "title": "Avoiding data sparsity issues",
      "body": "MLE in logistic regression gives infinite estimates when classes are perfectly separable (complete separation). A Gaussian prior (MAP) regularizes these estimates to finite values. Similarly, in sparse data settings (few observations per parameter), MAP with a regularizing prior prevents overfitting that MLE produces. It's the default in Bayesian logistic regression."
    }
  ],
  "deepDiveIntro": [
    "MAP estimation finds the parameter value that maximises the posterior, which combines the log-likelihood with the log-prior. This is conceptually the same as maximum likelihood estimation but with an extra term that penalises implausible parameter values. At the optimal point, the gradient of the data fit is exactly cancelled by the gradient of the log-prior.",
    "A key difference from MLE is that MAP estimates are not invariant to reparametrisation. Transforming the parameters changes the prior through a Jacobian factor, so the mode of the posterior shifts. The posterior mean and median, by contrast, are transformation-invariant in the full Bayesian sense but require integrating over the entire posterior rather than just finding its peak."
  ],
  "deepDive": [
    {
      "heading": "MAP with Gaussian Prior = Ridge Regression",
      "body": "A Gaussian prior turns maximum a posteriori estimation into ridge regression. The prior favors smaller coefficients, so the resulting estimate balances data fit against shrinkage toward zero. This is the clearest illustration of how Bayesian priors and classical regularization penalties are two views of the same idea.",
      "formula": "θ̂_MAP = (X'X + λI)⁻¹ X'y,  λ = σ²/τ²  [Ridge]"
    },
    {
      "heading": "MAP with Laplace Prior = Lasso",
      "body": "A Laplace prior leads to a penalty that encourages exact zeros, which is why MAP estimation with this prior matches the lasso. The resulting estimate does not just shrink coefficients; it can remove some of them entirely. That makes MAP useful when prediction and variable selection are both important.",
      "formula": "θ̂_MAP = argmin ||y − Xθ||² + λ||θ||₁  [Lasso]"
    },
    {
      "heading": "Posterior Mode vs Mean vs Median",
      "body": "The posterior mode is only one possible summary of a posterior distribution. In symmetric posteriors it agrees with the mean and median, but in skewed posteriors those summaries can differ substantially. Which summary is best depends on the loss function and on whether you care more about a single best point or about averaging over uncertainty."
    },
    {
      "heading": "Laplace Approximation Around MAP",
      "body": "The Laplace approximation turns a MAP solution into a local Gaussian approximation to the full posterior. It is most useful when full Bayesian computation is too expensive but a rough uncertainty estimate is still needed. The idea works best when the posterior is smooth, unimodal, and reasonably close to bell-shaped near its peak.",
      "formula": "Posterior ≈ N(θ̂_MAP, [−H]⁻¹)"
    }
  ],
  "youtubeLabel": "MAP vs MLE — Mutual Information",
  "youtubeUrl": "https://www.youtube.com/embed/kkhdIriddSI",
  "takeaways": [
    "θ̂_MAP = argmax [log P(x|θ) + log P(θ)]: MLE + log-prior regularization. [1]",
    "Gaussian prior → Ridge regression; Laplace prior → Lasso. λ encodes prior strength. [1]",
    "MAP is NOT reparametrization-invariant, unlike MLE. g(θ̂_MAP) ≠ MAP for g(θ). [1]",
    "Posterior mean minimizes squared error loss; MAP minimizes 0-1 loss; posterior median minimizes absolute error. [1]",
    "Laplace approximation: posterior ≈ N(θ̂_MAP, [−H]⁻¹) — enables uncertainty estimation without MCMC. [1]"
  ],
  "edgeCases": [
    {
      "q": "Does MAP always give a better estimate than MLE?",
      "a": "Not necessarily. MAP is better when the prior is informative and correct (or approximately correct), but can be worse than MLE if the prior is misspecified — biasing estimates away from the truth. For very large n, MLE and MAP converge to the same estimate (the likelihood overwhelms the prior). MAP is most valuable for small n or high-dimensional settings where regularization reduces variance without too much bias. [1]"
    },
    {
      "q": "What happens to MAP in high dimensions?",
      "a": "In high dimensions (p >> 1), the posterior mode (MAP) becomes an increasingly poor summary of the posterior — the typical set (where most posterior mass lives) moves away from the mode in high-dimensional Gaussians (the shell of highest probability is at radius √d from the mean, not at the center). Full MCMC or variational inference better captures the posterior geometry. MAP overfits in Bayesian neural networks with millions of parameters. [1]"
    }
  ],
  "sources": [
    { "label": "Bishop — Pattern Recognition and Machine Learning, Ch. 3, 4", "url": "https://www.microsoft.com/en-us/research/uploads/prod/2006/01/Bishop-Pattern-Recognition-and-Machine-Learning-2006.pdf" },
    { "label": "Gelman et al. — Bayesian Data Analysis (3rd ed.), Ch. 4", "url": "http://www.stat.columbia.edu/~gelman/book/" }
  ]
}
