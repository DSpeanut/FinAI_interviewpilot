{
  "visualizationType": "network-diagram",
  "codeSnapshot": {
    "label": "mdp.py",
    "code": "import numpy as np\n\n# ── 1. Define an MDP ──\n# States: 0=A, 1=B, 2=C (terminal)\n# Actions: 0=left, 1=right\n# Rewards: received on transition\n# Transition: P[s, a, s'] = probability of going to s' from s with action a\n\nn_states = 3\nn_actions = 2\ngamma = 0.9   # discount factor\n\n# Transition probabilities P(s'|s,a)\nP = np.zeros((n_states, n_actions, n_states))\nP[0, 0, 0] = 1.0    # from A, left → stay in A\nP[0, 1, 1] = 1.0    # from A, right → go to B\nP[1, 0, 0] = 1.0    # from B, left → go to A\nP[1, 1, 2] = 1.0    # from B, right → go to C (terminal)\nP[2, :, 2] = 1.0    # terminal state loops\n\n# Rewards R(s, a)\nR = np.array([\n    [-1, 0],    # from A: left=-1, right=0\n    [0,  10],   # from B: left=0, right=10\n    [0,  0],    # terminal: 0\n])\n\n# ── 2. Value Iteration ──\nV = np.zeros(n_states)\nfor _ in range(1000):\n    V_new = np.zeros(n_states)\n    for s in range(n_states - 1):   # skip terminal\n        Q_values = [\n            sum(P[s, a, s_next] * (R[s, a] + gamma * V[s_next])\n                for s_next in range(n_states))\n            for a in range(n_actions)\n        ]\n        V_new[s] = max(Q_values)\n    if np.max(np.abs(V_new - V)) < 1e-6:\n        break\n    V = V_new\n\nprint('Optimal value function V*:', V.round(3))\n\n# ── 3. Extract optimal policy ──\npolicy = np.zeros(n_states, dtype=int)\nfor s in range(n_states - 1):\n    Q_values = [\n        sum(P[s, a, s_next] * (R[s, a] + gamma * V[s_next])\n            for s_next in range(n_states))\n        for a in range(n_actions)\n    ]\n    policy[s] = np.argmax(Q_values)\n\nprint('Optimal policy π*:', policy)   # 0=left, 1=right\n\n# ── 4. Policy Evaluation (for a fixed policy π) ──\npi = np.array([1, 1, 0])   # always go right\nV_pi = np.zeros(n_states)\nfor _ in range(1000):\n    V_pi_new = np.array([\n        sum(P[s, pi[s], s_next] * (R[s, pi[s]] + gamma * V_pi[s_next])\n            for s_next in range(n_states))\n        for s in range(n_states)\n    ])\n    if np.max(np.abs(V_pi_new - V_pi)) < 1e-6:\n        break\n    V_pi = V_pi_new\nprint('Value under always-right policy:', V_pi.round(3))"
  },
  "eli3": [
    "A Markov Decision Process (MDP) is the mathematical framework that describes how an agent makes decisions in an environment. The world is described by states (where you are), actions (what you can do), rewards (what you get), and transitions (what happens after each action). The 'Markov' property means the future depends only on the current state, not on how you got there.",
    "The agent's goal is to find an optimal policy — a rule mapping every state to the best action — that maximises the total future reward, discounted by a factor γ (so near-term rewards count more than distant ones). The value function V(s) tells you: 'if you follow the optimal policy starting from state s, how much total reward should you expect?'",
    "Value Iteration is one way to compute the optimal value function: start with V(s)=0 everywhere, then repeatedly update V(s) = max_a [R(s,a) + γ · Σ P(s'|s,a) · V(s')] until convergence. This is guaranteed to find the optimal solution because the Bellman optimality equation has a unique fixed point. Policy Iteration (alternating between policy evaluation and policy improvement) is an alternative that often converges in fewer iterations."
  ],
  "whenWhy": [
    {
      "title": "Sequential decision-making under uncertainty",
      "body": "MDPs model any problem where an agent takes a sequence of actions, receives rewards, and the environment state changes stochastically in response. Applications: robot navigation (state = position, action = move direction), game playing (state = board position, action = move), inventory management (state = stock level, action = reorder quantity), clinical treatment planning (state = patient condition, action = treatment). Whenever decisions have delayed consequences — the right action now might lead to a better state later — MDPs are the right framework."
    },
    {
      "title": "Foundation for all of Reinforcement Learning",
      "body": "Every major RL algorithm — Q-learning, SARSA, Policy Gradient, Actor-Critic, PPO, DQN — is solving an MDP (or a Partially Observable MDP). Understanding MDPs is prerequisite to understanding any RL algorithm. The Bellman equations (optimality and expectation versions) appear in the derivation of every RL update rule. If you can formulate your problem as an MDP, you can immediately apply the entire RL toolkit."
    },
    {
      "title": "Planning when the model is known",
      "body": "When the transition probabilities P(s'|s,a) and rewards R(s,a) are known (model-based setting), MDP planning algorithms (Value Iteration, Policy Iteration) directly compute the optimal policy without any trial-and-error learning. This is used in: operations research (supply chain optimization, routing), classical game AI (chess endgame tablebases), and any domain where a simulator of the environment is available. Model-free RL (Q-learning, policy gradient) is used when P and R are unknown and must be estimated through experience."
    }
  ],
  "deepDiveIntro": [
    "An MDP is defined by its state space, action space, transition probabilities, reward function, and discount factor. The Markov property means the next state depends only on the current state and action, not on the full history. A policy maps states to actions (deterministic) or to probability distributions over actions (stochastic). The value of a state under a policy is the expected total discounted reward obtained by following that policy from that state. [1]",
    "The Bellman optimality equation says the optimal value of a state equals the best expected immediate reward plus the discounted optimal value of the resulting state, maximized over all actions. This has a unique solution, and Value Iteration finds it by repeatedly applying the update rule until the values stop changing. Policy Iteration alternates between evaluating the current policy exactly and improving it greedily, converging in a finite number of steps. [2]"
  ],
  "deepDive": [
    {
      "heading": "Bellman Equations",
      "body": "This is a non-linear system — no closed form, but solvable by Value Iteration (repeated application of the Bellman operator T).",
      "formula": "V*(s) = max_a [R(s,a) + γ Σ_{s'} P(s'|s,a) V*(s')]"
    },
    {
      "heading": "Value Iteration vs Policy Iteration",
      "body": "Value Iteration applies the Bellman update to every state repeatedly until the values converge. It is simple to implement but converges only asymptotically. Policy Iteration evaluates the current policy by solving a linear system exactly, then improves it by choosing the greedy action at each state. Each full iteration is more expensive but the algorithm terminates in a finite number of steps for finite state and action spaces."
    },
    {
      "heading": "Discount Factor γ and Return",
      "body": "Interpretation: γ close to 1 means the agent cares about distant future rewards (long-horizon planning). γ close to 0 means greedy (only immediate reward matters). γ is also a mathematical convenience: it ensures the Bellman operator is a contraction (convergence guarantee)."
    }
  ],
  "youtubeLabel": "Markov Decision Processes — David Silver RL Lecture 2",
  "youtubeUrl": "https://www.youtube.com/embed/lfHX2hHRMVQ",
  "takeaways": [
    "MDP = ⟨S, A, P, R, γ⟩. Markov property: future depends only on current state, not history. [1]",
    "Bellman optimality: V*(s) = max_a [R(s,a) + γ Σ_{s'} P(s'|s,a) V*(s')]. Unique solution. [1]",
    "Value Iteration: apply Bellman operator until convergence (γ-contraction → guaranteed). [2]",
    "Policy Iteration: evaluate policy (linear solve) + improve policy. Fewer iterations, costlier each step. [2]",
    "γ near 1 = long-horizon planner. γ near 0 = greedy. γ<1 ensures finite returns. [1]"
  ],
  "edgeCases": [
    {
      "q": "What happens when the state space is too large for tabular Value Iteration?",
      "a": "Tabular Value Iteration stores V(s) for every state — infeasible when |S| is exponentially large (e.g., all possible board positions in Go: ~10¹⁷⁰). Solutions: (1) Function approximation: parameterize V^π(s) ≈ V̂(s; θ) (linear features, neural network). This leads to approximate DP and eventually Deep RL (DQN). (2) Model-free RL (Q-learning, TD learning): instead of knowing P, estimate Q*(s,a) from experience — no need to store the full model. (3) Monte Carlo Tree Search (MCTS): plan forward from the current state using rollouts, focusing computation on reachable states rather than the entire state space. AlphaGo combines MCTS with neural network value/policy approximation to handle Go's massive state space."
    },
    {
      "q": "What is a Partially Observable MDP (POMDP) and why is it harder?",
      "a": "In a POMDP, the agent cannot directly observe the true state — it receives an observation o that is a noisy function of the true state. The agent must maintain a belief state b(s) = P(S_t=s | o_1,...,o_t, a_1,...,a_{t-1}) — a probability distribution over states. The belief state is the 'state' for decision-making, but the belief space is infinite (continuous probability simplex over |S| states). Optimal POMDP planning is PSPACE-complete. Practical approaches: (1) Point-based value iteration (PBVI): approximate the value function over a subset of belief points. (2) Deep RL with memory: use LSTM or attention to encode history implicitly — the hidden state approximates the belief state. (3) Particle filters for belief tracking + RL on top. POMDPs model real-world problems: robot localization (noisy sensors), medical diagnosis (incomplete information), autonomous driving (occluded objects)."
    }
  ],
  "sources": [
    {
      "label": "Sutton, R.S. & Barto, A.G. — Reinforcement Learning: An Introduction, 2nd Ed., MIT Press, 2018",
      "url": "http://incompleteideas.net/book/the-book-2nd.html"
    },
    {
      "label": "Puterman, M.L. — Markov Decision Processes: Discrete Stochastic Dynamic Programming, Wiley, 1994",
      "url": "https://doi.org/10.1002/9780470316887"
    }
  ]
}
