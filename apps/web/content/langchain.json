{
  "visualizationType": "langchain-viz",
  "eli3": [
    "LangChain is a Python and JavaScript library that gives you pre-built building blocks for LLM applications: document loaders, text splitters, vector stores, prompt templates, chains, and agents. Instead of writing everything from scratch, you snap these blocks together.",
    "Think of it like LEGO for LLM apps. Loading a PDF, splitting it into chunks, embedding it, storing in a vector database, and answering questions over it would take days to build manually. LangChain gives you pre-built pieces for each step that plug together in a few lines of code.",
    "LangChain has evolved from a 'chain' model (pre-built linear sequences) to LangGraph (a graph-based model for complex agent workflows with cycles and branching). Both are maintained by LangChain Inc. and are the most widely used LLM framework as of 2024."
  ],
  "whenWhy": [
    {
      "title": "Rapid prototyping of RAG and agent applications",
      "body": "LangChain's primary value is speed to prototype: load documents with a DocumentLoader, split with a TextSplitter, embed with an Embeddings wrapper, store in a VectorStore, and answer questions with a RetrievalQA chain — all in ~20 lines of Python. The abstractions handle provider-specific API differences (OpenAI vs. Anthropic vs. Cohere) behind a unified interface. This makes it faster to switch LLM providers and faster to get a working prototype. The community ecosystem (hundreds of pre-built integrations) is a major asset."
    },
    {
      "title": "Building stateful, multi-step agents with LangGraph",
      "body": "LangChain's agent framework evolved into LangGraph for production use cases. LangGraph models the agent workflow as a directed graph (with cycles, not just DAGs): nodes are steps (LLM calls, tool calls, human checkpoints), edges are transitions (conditional based on step output). This explicit graph structure enables: persistent state across nodes, human-in-the-loop at any node, parallel node execution, and time-travel debugging (rewind to any prior state). LangGraph is the recommended pattern for production agents over the older AgentExecutor."
    },
    {
      "title": "Standardised provider-agnostic development",
      "body": "LangChain's provider abstraction (ChatOpenAI, ChatAnthropic, ChatGoogleGenerativeAI all expose the same .invoke() interface) allows switching LLM providers by changing one line. This is valuable for: A/B testing providers, fallback to a secondary provider on API errors, mixing providers in multi-agent systems (e.g., GPT-4 for planning, Claude for writing, Gemini for multimodal). The same abstraction applies to vector stores, embeddings, and document loaders."
    }
  ],
  "deepDiveIntro": [
    "LangChain is a composable LLM application framework released in October 2022 by Harrison Chase. It defines the Runnable protocol — a universal interface that any component must implement — exposing .invoke(), .stream(), and .batch() methods. Every LangChain component (prompt templates, LLM wrappers, output parsers, retrievers, tools) is a Runnable, making them interchangeable and composable. LangChain Expression Language (LCEL) formalises this composability using Python's | operator: chain = prompt | llm | output_parser. Each | call wraps the left and right Runnables into a RunnableSequence that forwards inputs and outputs automatically, with built-in streaming, batching, and async support without any extra code. [1]",
    "LangChain's package structure reflects its maturity: LangChain Core defines the Runnable protocol and base abstractions; LangChain Community houses third-party integrations (vector stores, document loaders, LLM wrappers) contributed by the open-source community; the main langchain package provides first-party chain implementations. LangGraph, shipped as a separate package, extends the framework from linear chains to cyclic state machines suited to agentic workflows. LangSmith, the commercial observability layer, provides tracing and evaluation for production deployments. This layered architecture allows projects to depend only on Core for minimal footprint or pull in the full ecosystem for rapid prototyping. [1]"
  ],
  "deepDive": [
    {
      "heading": "LCEL: Composing Chains with the Pipe Operator",
      "body": "LangChain Expression Language (LCEL) uses Python's | operator to compose Runnables: chain = prompt | llm | output_parser. Each step in the chain must implement the Runnable protocol (.invoke(), .stream(), .batch()). LCEL chains are automatically parallelised when steps are independent (using RunnableParallel), support streaming out of the box (the | operator forwards stream events end-to-end), and integrate with LangSmith for automatic tracing. Compared to the older class-based Chains, LCEL is more composable and easier to debug. [1]"
    },
    {
      "heading": "LangGraph: State Machines for Agents",
      "body": "LangGraph models agent workflows as state machines: (1) State — a typed dictionary updated by each node; (2) Nodes — Python functions that take the current state and return a state update; (3) Edges — transitions between nodes (unconditional: always go to next_node; conditional: go to node_A if condition else node_B); (4) Checkpointing — persist state after each node using a checkpointer (SQLite, Redis, Postgres) for resumability. Human-in-the-loop: add an interrupt_before=['tool_call_node'] to pause execution before a tool is called and wait for human approval. Time-travel: use thread_id + checkpoint_id to rewind and re-execute from any prior state. [1]"
    },
    {
      "heading": "When Not to Use LangChain",
      "body": "LangChain adds abstraction overhead that is unnecessary for simple use cases: a single LLM call that doesn't need memory, tools, or retrieval is better done directly with the provider SDK (fewer dependencies, less magic, easier debugging). LangChain critics cite: (1) frequent breaking changes (v0.1 → v0.2 → v0.3 introduced significant API churn); (2) over-abstraction making it hard to understand what's happening under the hood; (3) large dependency tree. For production systems, many teams use LangChain for prototyping and then implement the critical path directly against provider SDKs. Use LangChain when the ecosystem integrations (document loaders, vector store connectors) save significant time; use the provider SDK directly for latency-sensitive paths. [1]"
    },
    {
      "heading": "LangGraph: Stateful Agent Workflows",
      "body": "LangGraph's production agent pattern centres on three constructs working together: a typed State dataclass (using Python's TypedDict or dataclass), a StateGraph builder that registers nodes and edges, and a compiled graph (graph.compile(checkpointer=...)) that handles execution. Nodes are pure Python functions — easily unit-tested in isolation. Edges can be unconditional (always route to the same next node) or conditional (inspect state and return the name of the next node). Sub-graphs allow encapsulating a complete workflow as a single node inside a parent graph, enabling hierarchical agent composition. LangGraph's persistence layer (checkpointers backed by SQLite, Redis, or Postgres) serialises the entire state dictionary after every node, enabling agents to resume mid-run after failures, support human review of intermediate outputs, and implement time-travel debugging by rewinding to any prior checkpoint. This persistence model is LangGraph's primary differentiator over simpler agent loops. [1]"
    }
  ],
  "takeaways": [
    "LangChain: pre-built components (loaders, splitters, retrievers, chains, agents) with a unified provider-agnostic interface. [1]",
    "LCEL: compose Runnables with the | operator — automatic streaming, batching, and parallelisation. [1]",
    "LangGraph: graph-based stateful agents with checkpointing, human-in-the-loop, and time-travel debugging. [1]",
    "Provider abstraction allows switching between OpenAI / Anthropic / Google by changing one line. [1]",
    "Avoid over-engineering simple single-LLM-call applications with LangChain — use the provider SDK directly. [1]"
  ],
  "edgeCases": [
    {
      "q": "How do you debug a complex LangChain/LangGraph pipeline when something goes wrong?",
      "a": "Debugging strategies: (1) LangSmith tracing — enables automatic logging of every LLM call, tool invocation, and intermediate state in a LangGraph run. Every run gets a trace URL; you can inspect inputs/outputs at each node. Set LANGCHAIN_TRACING_V2=true and LANGCHAIN_API_KEY in environment; (2) LangGraph time-travel — if a run fails at node N, rewind to node N-1 using the checkpoint, fix the state, and re-run from that point without restarting from scratch; (3) .invoke() with config={'callbacks': [ConsoleCallbackHandler()]} — verbose logging to stdout without LangSmith; (4) Unit testing individual nodes — since LangGraph nodes are just Python functions that take state → state_update, test them in isolation. The graph structure makes it easy to mock adjacent nodes."
    },
    {
      "q": "What's the difference between LangChain Agents (AgentExecutor) and LangGraph?",
      "a": "AgentExecutor (older): a fixed loop — call LLM, parse output, execute tools if needed, repeat. Limited: no branching, no persistent state across restarts, no human-in-the-loop, difficult to customise the loop. LangGraph: a flexible state machine where you define the loop structure explicitly. Advantages: (1) custom control flow — add human approval nodes, retry nodes, error-handling branches; (2) persistent state — survives restarts, resumes from checkpoints; (3) multi-agent — multiple agents as nodes in the same graph, passing state between them; (4) streaming — stream graph execution events to the client in real-time. LangChain's own documentation now recommends LangGraph over AgentExecutor for all new agent development."
    }
  ],
  "codeSnapshot": {
    "label": "langchain_chain.py",
    "code": "from langchain_core.prompts import ChatPromptTemplate\nfrom langchain_core.output_parsers import StrOutputParser\nfrom langchain_openai import ChatOpenAI\n\n# 1. Define a prompt template with input variables\nprompt = ChatPromptTemplate.from_messages([\n    (\"system\", \"You are a helpful assistant that explains technical concepts clearly.\"),\n    (\"human\", \"Explain {topic} in 2-3 sentences for a software engineer.\")\n])\n\n# 2. Initialise the LLM\nllm = ChatOpenAI(model=\"gpt-4o\", temperature=0)\n\n# 3. Add an output parser to extract plain text\nparser = StrOutputParser()\n\n# 4. Compose the chain using LCEL pipe operator\nchain = prompt | llm | parser\n\n# 5. Invoke (single synchronous call)\nresult = chain.invoke({\"topic\": \"LangChain\"})\nprint(result)\n\n# 6. Stream (token-by-token output)\nfor chunk in chain.stream({\"topic\": \"LangChain\"}):\n    print(chunk, end=\"\", flush=True)\nprint()  # newline after streaming completes\n\n# 7. Batch (multiple inputs in parallel)\nresults = chain.batch([\n    {\"topic\": \"LangChain\"},\n    {\"topic\": \"LangGraph\"},\n    {\"topic\": \"LangSmith\"},\n])\nfor r in results:\n    print(r)\n"
  },
  "deepDive": [
    {
      "heading": "LCEL: Composing Chains with the Pipe Operator",
      "body": "LangChain Expression Language (LCEL) uses Python's | operator to compose Runnables: chain = prompt | llm | output_parser. Each step in the chain must implement the Runnable protocol (.invoke(), .stream(), .batch()). LCEL chains are automatically parallelised when steps are independent (using RunnableParallel), support streaming out of the box (the | operator forwards stream events end-to-end), and integrate with LangSmith for automatic tracing. Compared to the older class-based Chains, LCEL is more composable and easier to debug. [1]"
    },
    {
      "heading": "LangGraph: State Machines for Agents",
      "body": "LangGraph models agent workflows as state machines: (1) State — a typed dictionary updated by each node; (2) Nodes — Python functions that take the current state and return a state update; (3) Edges — transitions between nodes (unconditional: always go to next_node; conditional: go to node_A if condition else node_B); (4) Checkpointing — persist state after each node using a checkpointer (SQLite, Redis, Postgres) for resumability. Human-in-the-loop: add an interrupt_before=['tool_call_node'] to pause execution before a tool is called and wait for human approval. Time-travel: use thread_id + checkpoint_id to rewind and re-execute from any prior state. [1]"
    },
    {
      "heading": "When Not to Use LangChain",
      "body": "LangChain adds abstraction overhead that is unnecessary for simple use cases: a single LLM call that doesn't need memory, tools, or retrieval is better done directly with the provider SDK (fewer dependencies, less magic, easier debugging). LangChain critics cite: (1) frequent breaking changes (v0.1 → v0.2 → v0.3 introduced significant API churn); (2) over-abstraction making it hard to understand what's happening under the hood; (3) large dependency tree. For production systems, many teams use LangChain for prototyping and then implement the critical path directly against provider SDKs. Use LangChain when the ecosystem integrations (document loaders, vector store connectors) save significant time; use the provider SDK directly for latency-sensitive paths. [1]"
    },
    {
      "heading": "LangGraph: Stateful Agent Workflows",
      "body": "LangGraph's production agent pattern centres on three constructs working together: a typed State dataclass (using Python's TypedDict or dataclass), a StateGraph builder that registers nodes and edges, and a compiled graph (graph.compile(checkpointer=...)) that handles execution. Nodes are pure Python functions — easily unit-tested in isolation. Edges can be unconditional (always route to the same next node) or conditional (inspect state and return the name of the next node). Sub-graphs allow encapsulating a complete workflow as a single node inside a parent graph, enabling hierarchical agent composition. LangGraph's persistence layer (checkpointers backed by SQLite, Redis, or Postgres) serialises the entire state dictionary after every node, enabling agents to resume mid-run after failures, support human review of intermediate outputs, and implement time-travel debugging by rewinding to any prior checkpoint. This persistence model is LangGraph's primary differentiator over simpler agent loops. [1]"
    },
    {
      "heading": "LCEL Runnables and the Composability Model",
      "body": "Every LCEL component implements the Runnable interface, exposing four core methods: invoke (single synchronous call), batch (multiple inputs in parallel, with configurable max_concurrency), stream (token-by-token generator), and astream (async generator for async contexts). This uniform interface means any Runnable can be swapped for another — a ChatOpenAI can be replaced with ChatAnthropic without changing the rest of the chain. Beyond simple sequences, LCEL provides three composition primitives: RunnableParallel executes multiple Runnables concurrently with the same input, returning a dict of results — useful for fan-out (run retriever + keyword extractor simultaneously); RunnablePassthrough forwards the input unchanged to the next step, useful for injecting the original query alongside retrieved context; RunnableLambda wraps any Python function as a Runnable, enabling custom preprocessing or postprocessing steps without subclassing. These primitives compose into arbitrary DAGs: chain = RunnableParallel({'context': retriever, 'question': RunnablePassthrough()}) | prompt | llm | parser. The | operator builds a RunnableSequence that propagates streaming and async support transparently throughout the entire graph. [1]"
    },
    {
      "heading": "LangSmith Tracing Integration",
      "body": "LangSmith is LangChain's observability platform that automatically traces all LCEL chain executions when the environment variable LANGCHAIN_TRACING_V2=true is set alongside a valid LANGCHAIN_API_KEY. No code changes are required — the tracing hooks are built into the Runnable protocol. Every trace captures: the full prompt sent to the LLM (after template rendering), the raw completion, per-step latency, token counts (input and output) for each LLM call, and any errors with their stack traces. For multi-step chains, each step appears as a nested span, making it easy to identify which step is slow or incorrect. To add custom metadata to a trace — for example, to tag a run with a user ID or experiment name — use chain.with_config({'metadata': {'user_id': '123', 'experiment': 'v2'}, 'run_name': 'my_chain_run'}). In production debugging, LangSmith traces are invaluable: when a user reports a wrong answer, retrieve the trace by run ID, inspect the exact prompt that was sent, compare retrieved context against the expected context, and identify whether the failure is in retrieval, prompt construction, or LLM reasoning. LangSmith also supports dataset-based evaluation: log production traces as a dataset, run a batch evaluation with an LLM judge, and track metric regressions across code changes. [1]"
    }
  ],
  "sources": [
    { "label": "LangChain Documentation — official docs and LCEL reference", "url": "https://python.langchain.com/docs/introduction/" },
    { "label": "LangGraph Documentation — official docs", "url": "https://langchain-ai.github.io/langgraph/" }
  ]
}
