{
  "visualizationType": "api-design-ai-viz",
  "codeSnapshot": {
    "label": "ai_api_client.py",
    "code": "import asyncio\nfrom langchain_openai import ChatOpenAI\nfrom langchain_core.messages import HumanMessage, SystemMessage\nfrom langchain_core.runnables import RunnableWithFallbacks\nfrom openai import RateLimitError\n\n# --- 1. Streaming ---\nllm_stream = ChatOpenAI(model=\"gpt-4o\", streaming=True)\n\nmessages = [HumanMessage(content=\"Explain transformers in 3 sentences.\")]\n\nprint(\"Streaming response:\")\nfor chunk in llm_stream.stream(messages):\n    print(chunk.content, end=\"\", flush=True)\nprint()  # newline after stream\n\n# --- 2. Rate limiting and retry ---\nllm_retry = ChatOpenAI(\n    model=\"gpt-4o\",\n    max_retries=3,          # exponential backoff on transient errors\n    request_timeout=30      # seconds before timeout\n)\n\n# --- 3. Token budget: count before sending ---\nllm_counter = ChatOpenAI(model=\"gpt-4o\")\n\nmessages_to_send = [\n    SystemMessage(content=\"You are a helpful assistant.\"),\n    HumanMessage(content=\"Summarise the following document: ...\")\n]\n\nprompt_tokens = llm_counter.get_num_tokens_from_messages(messages_to_send)\nMAX_INPUT_TOKENS = 4096\n\nif prompt_tokens > MAX_INPUT_TOKENS:\n    raise ValueError(\n        f\"Prompt too large: {prompt_tokens} tokens (limit: {MAX_INPUT_TOKENS})\"\n    )\n\nresponse = llm_counter.invoke(messages_to_send)\nprint(f\"Response: {response.content[:100]}...\")\n\n# --- 4. Fallback chain ---\nprimary_llm = ChatOpenAI(model=\"gpt-4o\")\nfallback_llm = ChatOpenAI(model=\"gpt-4o-mini\")\n\n# Falls back to gpt-4o-mini if primary raises RateLimitError\nrobust_llm: RunnableWithFallbacks = primary_llm.with_fallbacks(\n    [fallback_llm],\n    exceptions_to_handle=(RateLimitError,)\n)\n\nresult = robust_llm.invoke([HumanMessage(content=\"Hello, world!\")])\nprint(f\"Fallback result: {result.content}\")\n"
  },
  "eli3": [
    "When you build an API for an AI-powered feature, you have to think about design challenges that don't exist in traditional APIs: responses take seconds (not milliseconds), responses may be streamed token-by-token, the same input can produce different outputs, and responses may be very long.",
    "Good AI API design handles these challenges explicitly: use streaming (Server-Sent Events or WebSockets) so users see partial responses immediately, design idempotency carefully (LLMs are not deterministic), and design endpoints around the AI task's natural unit (a conversation, a generation job) rather than standard CRUD.",
    "AI API design also requires careful attention to cost: unlike traditional APIs where more requests = more server cost, AI APIs have per-token costs that vary with input length. Rate limiting, token limits, and cost attribution are first-class concerns."
  ],
  "whenWhy": [
    {
      "title": "Streaming responses for low perceived latency",
      "body": "LLM generation latency is 1-30 seconds depending on response length. Without streaming, users stare at a loading spinner for the full duration. With streaming (SSE or WebSockets), the first token appears in < 500ms (time to first token, TTFT), and subsequent tokens appear as they're generated. This dramatically improves perceived responsiveness. Server-Sent Events (SSE) are the standard for streaming LLM responses: the server sends text/event-stream events; the client processes them incrementally. All major LLM API providers (OpenAI, Anthropic, Google) use SSE for streaming."
    },
    {
      "title": "Job-based APIs for long-running generations",
      "body": "For very long generations (reports, code generation, multi-step analysis), a synchronous HTTP request-response pattern doesn't work — the connection may timeout. Job-based pattern: POST /generations returns a {job_id} immediately; the job runs asynchronously; GET /generations/{job_id}/status returns {status: 'in_progress' | 'complete' | 'failed'}; GET /generations/{job_id}/result returns the output when complete. Clients poll or receive a webhook. This decouples submission from retrieval and handles network interruptions gracefully."
    },
    {
      "title": "Cost control and token-aware rate limiting",
      "body": "AI APIs have per-token costs that dwarf compute costs for equivalent throughput in traditional services. API design must include: per-user token budgets (track input + output tokens per API key), rate limits by tokens per minute (not just requests per minute), context length limits (reject requests with oversized inputs), and cost attribution (log which user/feature consumed which tokens for billing and abuse detection). Token-aware rate limiting is more nuanced than request-rate limiting: one 10k-token request costs as much as 100 100-token requests."
    }
  ],
  "deepDiveIntro": [
    "RESTful API design for AI services differs from CRUD APIs in three fundamental ways: (1) Statefulness — LLM conversations are inherently stateful (conversation history); API design must decide where state lives (client-managed, server-managed, or stateless with client-sent history); (2) Non-determinism — the same request may return different responses; idempotency keys don't guarantee identical responses (unlike payment APIs); (3) Latency — LLM responses are slow; synchronous request-response works for short generations; async job patterns are required for long ones.",
    "The OpenAI and Anthropic REST APIs are the industry reference designs. Both use: POST /messages (or /completions) for generation, streaming via SSE with stream=true, token usage in response metadata, and per-request model selection. Key design decision: stateless API (client sends full conversation history every request) vs. stateful (server manages conversation state by session_id). OpenAI and Anthropic use stateless design for simplicity and horizontal scalability. Stateful design simplifies clients but creates server-side storage scaling challenges."
  ],
  "deepDive": [
    {
      "heading": "Streaming with Server-Sent Events",
      "body": "SSE format: HTTP response with Content-Type: text/event-stream. Each event is: data: {json_payload}\\n\\n. For LLM streaming: each event carries a delta (the new tokens generated since the last event). Final event: data: [DONE]\\n\\n (OpenAI) or an event with stop_reason (Anthropic). Client implementation: EventSource API (browser) or requests + iter_lines() (Python). Important: set the response headers correctly: Content-Type: text/event-stream, Cache-Control: no-cache, X-Accel-Buffering: no (disables Nginx/proxy buffering that would delay events). Intermediate proxies (load balancers, API gateways) must support SSE — some buffer responses by default; configure stream pass-through. [1]"
    },
    {
      "heading": "Conversation State Management",
      "body": "Stateless (client sends history): every request includes the full conversation history as an array of messages. Simple to implement server-side; each request is independent (easy to load balance). Drawbacks: large requests for long conversations (high token cost); client responsible for history truncation. Stateless with server-side context caching: same as stateless, but the server caches the KV cache for the static prefix (system prompt + early conversation) to reduce recomputation cost. Anthropic's prompt caching uses this pattern. Stateful (server stores history): client sends only the new message; server retrieves history by conversation_id. Smaller request payloads; server handles truncation. Drawbacks: stateful server (harder to scale horizontally); conversation lifecycle management (expiry, storage cost). Most production systems use stateless for scalability but add caching for cost efficiency. [1]"
    },
    {
      "heading": "Token-Aware Rate Limiting and Cost Attribution",
      "body": "Implementation: (1) Token counting — tokenise the request before the LLM call using tiktoken (OpenAI) or count_tokens API; reject requests exceeding max_input_tokens; (2) Token-per-minute (TPM) limiting — maintain a sliding window counter per API key: if (input_tokens + estimated_output_tokens) > remaining_TPM, return 429 with retry_after; (3) Cost attribution — log per request: {user_id, feature_name, input_tokens, output_tokens, model, timestamp, cost_usd}. Aggregate daily/weekly for billing and anomaly detection; (4) Budget alerts — alert when a user's daily token spend exceeds a threshold; auto-suspend accounts exceeding limits. Estimated output tokens: use a simple heuristic (max_tokens / 2) for remaining TPM calculation since actual output tokens aren't known at request time. [2]"
    },
    {
      "heading": "Async and Concurrent API Calls",
      "body": "LangChain exposes async variants of all invocation methods: ainvoke (single async call), abatch (batch of inputs run concurrently), and astream (async streaming generator). Using asyncio.gather() to run multiple LLM calls concurrently: tasks = [llm.ainvoke(msg) for msg in message_list]; results = await asyncio.gather(*tasks). Performance gain: 10 sequential calls at 2s each = 20s total; 10 concurrent calls = ~2s total (network-bound). Rate limit considerations: concurrent calls still consume tokens-per-minute quota simultaneously — a burst of 10 parallel calls may exhaust TPM limits faster than sequential calls. Semaphore-based throttling to avoid 429 errors: semaphore = asyncio.Semaphore(5); async def call_with_limit(msg): async with semaphore: return await llm.ainvoke(msg). This caps concurrency at 5 in-flight requests at any moment, smoothing the burst load. LangChain's abatch also accepts a config parameter for concurrency control: llm.abatch(inputs, config={\"max_concurrency\": 5}). For production batch processing, combine semaphore-based throttling with exponential backoff retry to handle residual 429s. [1]"
    },
    {
      "heading": "Cost Attribution and Budget Controls",
      "body": "Tracking token usage per user, session, or feature enables billing, anomaly detection, and cost optimization. Accessing token counts in LangChain: response = llm.invoke(messages); usage = response.usage_metadata; input_tokens = usage['input_tokens']; output_tokens = usage['output_tokens']. Building a budget controller: maintain a cumulative cost table per user (Redis or DB). After each call, compute cost_usd = (input_tokens / 1_000_000) * input_price + (output_tokens / 1_000_000) * output_price; add to the user's running total; if total exceeds budget, raise BudgetExceededError before the next call. Per-model pricing reference (as of 2024): gpt-4o — $5 per 1M input tokens, $15 per 1M output tokens; gpt-4o-mini — $0.15 per 1M input, $0.60 per 1M output. OpenAI usage tiers (Tier 1-5) set TPM and RPM limits; higher spend unlocks higher limits. Cost optimization strategies: (1) Prompt caching — reuse the KV cache for static system prompts (Anthropic's cache_control, OpenAI's prompt caching for gpt-4o); (2) Model routing — use gpt-4o-mini for simple classification/extraction tasks, gpt-4o only for complex reasoning; (3) Output length limits — set max_tokens based on task; avoid open-ended generation for structured tasks. [2]"
    }
  ],
  "takeaways": [
    "Streaming (SSE): first token in < 500ms TTFT — essential for LLM APIs; configure proxies to not buffer. [1]",
    "Stateless history: client sends full conversation history; enables horizontal scaling and easy load balancing. [1]",
    "Job-based pattern: POST returns job_id immediately; poll/webhook for result — handles long generations. [2]",
    "Token-aware rate limits: TPM limits more meaningful than RPM for cost-sensitive AI APIs. [2]",
    "Cost attribution: log {user, feature, input_tokens, output_tokens, model, cost_usd} per request for billing and anomaly detection. [2]"
  ],
  "edgeCases": [
    {
      "q": "How do you handle a user who sends a maliciously large input to inflate your costs?",
      "a": "Input abuse vectors: (1) extremely long system prompts; (2) injecting the full text of books into user messages; (3) attaching very large files for processing. Defences at the API layer: (1) max_input_tokens hard limit — reject requests with input token count > limit before calling the LLM; (2) File upload size limits — validate file sizes at upload time (not at processing time); (3) Character-level pre-filters — quick string length check before tokenisation (approximate, but catches extreme cases fast); (4) Per-user daily token budgets — even if a single request is within limits, a user can exhaust your budget by making many requests; (5) CAPTCHA / proof-of-work for unauthenticated endpoints — prevents automated large-input attacks. Never rely on the LLM API's own rate limits as your only protection — they're per-account, not per-user."
    },
    {
      "q": "How do you design idempotency for AI API requests?",
      "a": "Traditional idempotency (payment APIs): same request + same idempotency key → same response. LLMs are non-deterministic: same input may produce different output even with temperature=0 (model updates, sampling differences). True idempotency requires caching: store (request_hash → response) and return the cached response for duplicate requests. Implementation: hash the full request (model + messages + parameters) with SHA-256; check cache before calling LLM; on miss, call LLM and store response. Cache TTL: 24-48 hours for most use cases. When NOT to cache: creative tasks where variety is desired; tasks where the user explicitly refreshes. Idempotency key semantics in AI APIs: use it to deduplicate exact-same requests (e.g., retry after network failure), not to guarantee determinism. Document this distinction clearly in your API contract."
    }
  ],
  "sources": [
    { "label": "OpenAI API Design Reference — streaming, tool use, rate limits", "url": "https://platform.openai.com/docs/api-reference" },
    { "label": "Anthropic API Reference — messages, streaming, token counting", "url": "https://docs.anthropic.com/en/api/getting-started" }
  ]
}
