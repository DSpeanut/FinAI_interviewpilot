{
  "visualizationType": "mcp-arch-viz",
  "eli3": [
    "MCP (Model Context Protocol) is an open standard created by Anthropic that defines how AI applications connect to external tools and data sources. Instead of every AI app having to write custom code to connect to Slack, GitHub, or a database, MCP provides one standard way to do it.",
    "Think of MCP like USB: before USB, every peripheral (keyboard, mouse, printer) needed a different port and driver. After USB, everything uses the same standard. MCP does the same for AI tools — write one MCP server for your database, and any MCP-compatible AI app can use it.",
    "MCP has three layers: the host (the AI app, like Claude Desktop or an agent framework), the client (manages the connection), and the server (exposes tools, resources, and prompts). The host communicates with servers over a standardised JSON-RPC protocol."
  ],
  "whenWhy": [
    {
      "title": "Building reusable tool integrations across AI applications",
      "body": "Without MCP: each AI application (Claude Desktop, Cursor, a custom agent) writes its own custom integration with each external tool (Slack, GitHub, PostgreSQL). With MCP: write one MCP server per tool; every MCP-compatible application gets access automatically. This ecosystem effect means the community (and vendors) can build MCP servers once and share them. Anthropic maintains a registry of community MCP servers covering hundreds of tools. AI application developers get integrations for free; tool vendors write one MCP server instead of N integrations."
    },
    {
      "title": "Giving AI agents secure, structured access to local data",
      "body": "MCP servers can run locally (on the user's machine), exposing local resources (file system, local databases, local applications) to AI models without those resources ever leaving the user's machine. This solves the privacy problem: you can give Claude access to your local code files via an MCP filesystem server without uploading your files to Anthropic. The host controls which MCP servers are connected and what permissions they have, maintaining user control over data access."
    },
    {
      "title": "Standardising context injection beyond tool calls",
      "body": "MCP supports three primitives beyond tools: (1) Resources — URI-addressable data that the LLM can read (file contents, database rows, API responses); (2) Prompts — reusable prompt templates with parameters; (3) Sampling — the server can request the LLM to complete a text, enabling server-side agentic behaviour. This richer protocol enables MCP servers to inject relevant context into the LLM's prompt, not just expose callable functions."
    }
  ],
  "deepDiveIntro": [
    "MCP (Model Context Protocol) is a standardised, open protocol introduced by Anthropic in November 2024 for connecting LLMs to external tools and data sources. The protocol defines a client-host-server architecture: the host is the AI application that embeds the LLM (Claude Desktop, Cursor, a custom agent framework); the client is a component embedded in the host that manages the MCP protocol connection; and the server is an independent process that declares its capabilities — tools (callable functions with JSON Schema parameters), resources (URI-addressable read-only data), and prompts (reusable named templates). By providing one universal protocol, MCP eliminates the N×M integration problem: instead of each AI application writing custom adapters for every external tool, each tool implements a single MCP server and every compatible host connects to it automatically.",
    "The protocol uses JSON-RPC 2.0 as its message format. The host communicates with servers via a standardised request-response cycle: capability discovery (initialize, listing tools/resources), tool invocation (tools/call with tool name and JSON arguments), and resource reads (resources/read). The transport layer is deliberately separate from the protocol semantics — local servers communicate over stdin/stdout (the host spawns the server as a subprocess), while remote servers use HTTP with Server-Sent Events for streaming. Anthropic provides official Python and TypeScript SDKs (FastMCP) that handle JSON Schema generation from type hints, transport negotiation, and message framing, leaving developers to focus on business logic. [1]"
  ],
  "deepDive": [
    {
      "heading": "MCP Architecture: Host, Client, Server",
      "body": "Host: the application that embeds the AI model (Claude Desktop, VS Code with Copilot, a custom agent). The host manages the lifecycle of MCP server connections. Client: embedded in the host, handles the protocol communication with each MCP server. One client per server connection. Server: an independent process that exposes tools, resources, and prompts over the MCP protocol. Servers are isolated processes — the host cannot access the server's internal state; it can only call declared tools and read declared resources. Security boundary: the server decides what to expose; the host decides which servers to connect to; the user configures which hosts and servers are allowed. [1]"
    },
    {
      "heading": "Implementing an MCP Server (Python SDK)",
      "body": "Minimal Python MCP server: import mcp.server.fastmcp; create a FastMCP instance; decorate functions with @mcp.tool() (with a docstring as the tool description) and @mcp.resource('resource://uri') for resources. The FastMCP framework handles: JSON Schema generation from Python type hints, JSON-RPC message parsing, stdio transport. Tool function signatures use Python type hints for parameter schema: def get_file(path: str) -> str: ... generates {name: 'get_file', inputSchema: {type: 'object', properties: {path: {type: 'string'}}}}. Run with mcp.run(transport='stdio'). The server is started by the host as a subprocess. [1]"
    },
    {
      "heading": "MCP Resources and the Context Protocol",
      "body": "Resources are URI-addressable data that MCP servers expose for AI models to read. Example resource URIs: file:///path/to/document.txt, db://table/users, github://repo/owner/repo. The host can list available resources (resources/list) and read them (resources/read). Unlike tools (which the LLM calls to take actions), resources are read-only context sources that the host injects into the LLM's context window. This enables use cases like: an MCP server that exposes the current state of a database as a readable resource, injected into the LLM's context at the start of each query. The distinction between tools (actions) and resources (context) mirrors the RAG architecture: resources are the retrieval layer; tools are the action layer. [1]"
    },
    {
      "heading": "MCP Transports: stdio, HTTP/SSE, and WebSocket",
      "body": "MCP is transport-agnostic: the JSON-RPC message format is identical regardless of how messages are delivered. stdio transport: the host spawns the MCP server as a local subprocess and communicates over stdin/stdout. Appropriate for local servers (filesystem, local databases, desktop apps). Zero network overhead; the server shares the user's OS session. HTTP + SSE transport: the MCP server is a remote HTTP service. The client sends requests as HTTP POST; the server streams responses (including long-running tool outputs) via Server-Sent Events on a persistent GET connection. Appropriate for hosted, shared MCP servers (SaaS tools, team-shared integrations). WebSocket transport (experimental): full-duplex persistent connection; suited for high-frequency bidirectional communication between host and server. Authentication: remote transports support OAuth 2.0 and bearer token auth in the HTTP headers. The official SDKs abstract transport selection behind a single run() call — swap transport without changing tool logic. [1]"
    }
  ],
  "takeaways": [
    "MCP: open standard for AI-to-tool connectivity — write one server per tool, works with all MCP-compatible hosts. [1]",
    "Three primitives: tools (callable functions), resources (readable context), prompts (reusable templates). [1]",
    "Local stdio transport: MCP servers run locally — sensitive data never leaves the user's machine. [1]",
    "JSON-RPC 2.0 over stdio (local) or HTTP+SSE (remote) — transport-agnostic protocol. [1]",
    "Python/TypeScript SDKs: FastMCP generates JSON Schema from type hints — minimal boilerplate. [1]"
  ],
  "edgeCases": [
    {
      "q": "How do you handle authentication and secrets in MCP servers?",
      "a": "MCP servers often need credentials to access external services (API keys, database passwords, OAuth tokens). Security guidelines: (1) Never hardcode credentials in MCP server source code — use environment variables or secret managers; the host spawns the server with environment variables set from a secure config; (2) OAuth 2.0 for remote MCP servers — MCP supports OAuth flows where the server redirects the user for authorization and exchanges the auth code for a token; the token is stored by the host and passed to the server; (3) Credential scoping — give each MCP server the minimum necessary permissions (read-only access if write is not needed); (4) Token rotation — for long-lived MCP connections, implement token refresh; (5) Audit logging — log all tool calls and resource reads by MCP servers for security monitoring. Never store credentials in the MCP server's state — they should be injected at startup via environment variables."
    },
    {
      "q": "What's the difference between MCP and LangChain tools?",
      "a": "LangChain tools are Python objects within the LangChain ecosystem — they only work with LangChain agents and applications. MCP servers are independent processes exposing a language-agnostic protocol — they work with any MCP-compatible host (Claude Desktop, Cursor, a custom agent written in any language). LangChain tools: tighter integration (no serialisation overhead, Python-native), but ecosystem-locked. MCP servers: more deployment complexity (separate process, stdio/HTTP transport), but universal compatibility. In practice: for production systems using LangChain/LangGraph, LangChain tools are simpler. For tools that should be reusable across multiple AI applications or exposed to Claude Desktop users, MCP is the right choice. The two are not mutually exclusive: a LangChain tool can be wrapped as an MCP server."
    }
  ],
  "sources": [
    { "label": "Anthropic MCP Documentation — official specification and SDKs", "url": "https://docs.anthropic.com/en/docs/mcp" },
    { "label": "MCP GitHub Repository — open-source specification and reference implementations", "url": "https://github.com/modelcontextprotocol/specification" }
  ]
}
