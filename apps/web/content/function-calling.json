{
  "visualizationType": "function-calling-viz",
  "codeSnapshot": {
    "label": "function_calling.py",
    "code": "from langchain_openai import ChatOpenAI\nfrom langchain_core.tools import tool\nfrom langchain_core.messages import HumanMessage, ToolMessage\nfrom langchain_core.utils.function_calling import convert_to_openai_tool\nfrom langchain_core.output_parsers.openai_tools import JsonOutputToolsParser\n\n# --- 1. Define a Python function as a tool ---\n@tool\ndef extract_event(title: str, date: str, location: str) -> dict:\n    \"\"\"Extract a calendar event from text. Returns structured event data.\n    Args:\n        title: The name or subject of the event.\n        date: The event date in YYYY-MM-DD format.\n        location: The venue or location of the event.\n    \"\"\"\n    return {\"title\": title, \"date\": date, \"location\": location}\n\n# --- 2. Convert to OpenAI tool schema ---\nopenai_tool_schema = convert_to_openai_tool(extract_event)\nprint(\"OpenAI tool schema:\")\nimport json\nprint(json.dumps(openai_tool_schema, indent=2))\n\n# --- 3. Bind the tool to ChatOpenAI and force the model to call it ---\nllm = ChatOpenAI(model=\"gpt-4o\", temperature=0)\nllm_with_tool = llm.bind_tools(\n    [extract_event],\n    tool_choice={\"type\": \"function\", \"function\": {\"name\": \"extract_event\"}},\n)\n\n# --- 4. Run extraction on unstructured text ---\ntext = \"The LangChain Summit is happening on 2025-09-15 at the Moscone Center in San Francisco.\"\nmessages = [HumanMessage(content=f\"Extract the event details from this text: {text}\")]\nresponse = llm_with_tool.invoke(messages)\nprint(\"\\nRaw LLM response:\")\nprint(response)\n\n# --- 5. Parse the tool call output into a Python dict ---\nparser = JsonOutputToolsParser()\nparsed = parser.invoke(response)\nprint(\"\\nParsed tool call output:\")\nprint(parsed)\n# Output: [{'type': 'extract_event', 'args': {'title': 'LangChain Summit', 'date': '2025-09-15', 'location': 'Moscone Center in San Francisco'}}]\n"
  },
  "eli3": [
    "Function calling is a feature in LLM APIs that lets the model say 'I want to call this specific function with these specific arguments' in a structured, machine-readable format. Instead of the model saying 'you should search for X', it outputs a JSON object that your code can actually execute.",
    "Before function calling, developers had to parse the model's text output to figure out what action it wanted to take — fragile and error-prone. Function calling gives you a guaranteed-structured output: {name: 'search', arguments: {query: 'Paris weather'}} — your code can act on this directly.",
    "Think of it as the LLM filling out a form instead of writing a letter. The form has fixed fields (function name, argument names) so your code always knows exactly where to find the information it needs."
  ],
  "whenWhy": [
    {
      "title": "Reliable tool invocation without prompt engineering",
      "body": "Before native function calling, developers used prompt templates ('If you want to search, output SEARCH: <query>') and regex parsing. This breaks when the model outputs the format slightly differently. Native function calling (OpenAI 2023, Anthropic tool use) gives a guaranteed structured output: the model produces valid JSON that matches the schema, or it doesn't call the function. This eliminates an entire class of parsing failures and makes tool-using agents reliable in production."
    },
    {
      "title": "Structured data extraction from unstructured text",
      "body": "Function calling is also used for extraction, not just tool invocation. Define a function with the schema of the data you want (e.g., {name: string, date: string, amount: number}) and instruct the model to call it with the extracted values. The model acts as a structured extractor, outputting clean JSON from messy input text. This pattern replaces complex regex/NLP extraction pipelines with a single LLM call."
    },
    {
      "title": "Parallel, type-safe, validated API integration",
      "body": "Function calling integrates with existing codebases: define your API methods as tool schemas, wire up the execution to the actual implementation, and the LLM becomes an intelligent orchestrator of your API. JSON Schema validation catches type mismatches before the function runs. Parallel function calls (multiple calls in one response) allow concurrent execution of independent operations. The result is an LLM that correctly invokes your code, not one that describes what you should do."
    }
  ],
  "deepDiveIntro": [
    "Function calling is the standardised API mechanism by which an LLM signals its intent to invoke an external function with a precisely specified set of arguments, expressed as a validated JSON object conforming to a developer-supplied JSON Schema. Rather than embedding tool invocations in free-form text (requiring fragile regex parsing), the model emits a discrete structured output — a tool_calls array — that the caller can parse, validate, and dispatch to the real function implementation. This clean separation between intent (the model's structured call) and execution (the caller's code) is what makes function calling the foundation of reliable, production-grade LLM applications.",
    "OpenAI introduced function calling in June 2023 (GPT-3.5-turbo and GPT-4). The API accepts a 'tools' array of function definitions (JSON Schema objects). The model can output a 'tool_calls' array in its response, each containing a function name and arguments JSON string. The caller parses and executes each call, then sends results back as 'tool' role messages. Anthropic's implementation (called 'tool use') is structurally identical but uses different field names ('tool_use' content block, 'tool_result' role). Critically, the model is fine-tuned — not merely prompted — to produce schema-conformant calls, making function calling substantially more reliable than prompt-engineering approaches. This fine-tuning also covers tool selection: given multiple tools, the model learns to choose the most appropriate one, or none, when no tool is needed. [1]"
  ],
  "deepDive": [
    {
      "heading": "Function Schema Design",
      "body": "Each function schema has three fields: (1) name — snake_case, verb-noun format (get_weather, create_event, send_email) — the LLM uses this for selection; (2) description — the most impactful field: explain what the function does, when to use it, when NOT to use it, and what it returns. Models with poor tool selection often have vague descriptions. Include examples in the description for complex tools; (3) parameters — JSON Schema object: list all parameters with their types, descriptions, constraints (enum, minimum, maximum), and mark required fields. Omit optional fields with sensible defaults from the schema to reduce LLM confusion. [1]"
    },
    {
      "heading": "tool_choice Parameter: Controlling Invocation",
      "body": "OpenAI and Anthropic both support a tool_choice (or similar) parameter: (1) auto (default) — the model decides whether to call a function or respond in text; (2) required — the model must call a function (useful when you always need structured output); (3) none — the model cannot call functions (useful for follow-up conversational turns); (4) specific function — force the model to call a particular function (useful for extraction tasks). Setting tool_choice='required' with a single extraction function reliably produces structured output without the model deciding to respond in text instead. [1]"
    },
    {
      "heading": "Parallel Function Calls and Execution",
      "body": "When the model emits multiple tool_calls in one response, execute them concurrently (asyncio.gather in Python, Promise.all in Node.js) to minimise latency. Each call has a unique tool_call_id. When sending results back, include the tool_call_id in each tool result message so the model can match responses to requests. Handle partial failures: if 3 of 5 parallel calls succeed, send all 5 results (including the 3 errors) back to the model. The model can then decide to retry the failed calls or proceed with partial information. [1]"
    },
    {
      "heading": "Parallel Function Calling and Batching",
      "body": "Modern LLM APIs support emitting multiple function calls in a single model response — a capability that significantly reduces round-trip latency for tasks with independent sub-queries. When the model determines that N calls can be made without any data dependency between them (e.g., fetching prices for 5 different assets, looking up 3 user profiles), it emits all N tool_call objects in one response. The caller executes them concurrently and returns all results in a single follow-up message, bundling each result with its corresponding tool_call_id. Batching decisions belong to the model: developers should not artificially restrict the tool schema to prevent parallel calls, as this increases wall-clock time. Instead, the framework must be built to handle partial batch failures gracefully — returning both successful and failed results to the model so it can decide whether to retry individual calls, proceed with partial data, or surface an error. For very high-fanout tasks (hundreds of calls), consider chunking into batches of 10–20 to stay within context window budgets and avoid rate limit exhaustion. [1]"
    },
    {
      "heading": "Strict Mode and Schema Validation",
      "body": "OpenAI introduced strict mode for function calling (available by setting `\"strict\": true` in the function definition) to eliminate a class of failures where the model generates arguments that technically parse as JSON but violate the schema in subtle ways. In strict mode, the API enforces two invariants: (1) all properties defined in the schema must be listed in `required` — no optional fields; (2) `additionalProperties` must be set to `false` — the model cannot emit keys not listed in the schema. These constraints allow the API to use constrained decoding (structured output generation guided by the schema grammar), which guarantees schema conformance with near-100% reliability, compared to ~95–98% for non-strict mode. The trade-off: strict mode is less flexible — schemas cannot have optional fields, which sometimes requires splitting one tool into multiple variants. Use strict mode whenever you need guaranteed schema conformance: structured data extraction pipelines, downstream systems that parse the JSON programmatically, and multi-step workflows where a schema violation in step N cascades into failures in steps N+1 onward. Use permissive mode when the schema evolves frequently, when optional fields are genuinely needed for expressiveness, or when you are iterating quickly during development and strict mode's schema requirements would add friction. Note that `convert_to_openai_tool` in LangChain does not add strict mode by default — you must set it manually in the tool definition dict if needed. [1]"
    },
    {
      "heading": "Multi-turn Function Calling",
      "body": "A single function calling exchange follows a four-message pattern: (1) user message with the request, (2) assistant message containing the tool_calls array, (3) tool message(s) containing the results (one per tool_call_id), (4) assistant message with the final natural language answer. Multi-turn function calling extends this pattern across multiple user turns, which requires maintaining the complete conversation history — including all intermediate assistant and tool messages — in the messages array passed to each API call. Omitting any intermediate tool message causes an API error (OpenAI requires that every tool_call in an assistant message is paired with a corresponding tool result message before the next user turn). The role='tool' messages (LangChain: `ToolMessage`) are structurally distinct from user and assistant messages: they must include the `tool_call_id` field matching the originating tool call, and their content is the serialized tool result (typically a string, even if the original result was a dict). A correct multi-turn loop: maintain a `messages` list, append the assistant response (including tool calls), execute each tool, append a ToolMessage for each result, then either get the next user input and append a HumanMessage, or call the LLM again if more tool calls are needed. Conversation memory for tool-using agents must preserve the full interleaved history — systems that summarize or truncate history must keep assistant+tool message pairs together, never summarizing across a boundary that would drop a tool result without its paired tool call. [1]"
    }
  ],
  "takeaways": [
    "Function calling produces guaranteed-structured JSON outputs for tool invocation, replacing fragile regex parsing. [1]",
    "The description field in the function schema is the primary signal the model uses for tool selection — make it precise. [1]",
    "tool_choice='required' forces structured output; use it for extraction tasks where the model must always call the function. [1]",
    "Parallel function calls reduce latency; execute concurrently and match results to requests via tool_call_id. [1]",
    "Models are fine-tuned to produce schema-valid calls — more reliable than prompt-engineering JSON output. [1]"
  ],
  "edgeCases": [
    {
      "q": "How do you handle function arguments that are too long (e.g., passing a large document as an argument)?",
      "a": "Never pass large documents as function arguments — the arguments are in the model's context window (counted against the limit) and large arguments consume tokens that could be used for reasoning. Instead: (1) store the document externally (S3, database) and pass only a document_id to the function; the function implementation retrieves the document; (2) pre-process the document into a summary or structured form before the LLM call; (3) for RAG, pass a retrieval query as the argument, not the document content. The function arguments should contain only the minimal parameters needed for the function to execute — not the data itself."
    },
    {
      "q": "What if the model calls a function with invalid argument values (e.g., wrong date format, out-of-range number)?",
      "a": "Validate arguments server-side before execution — never trust that the LLM's JSON conforms to your constraints just because it matches the JSON Schema type. JSON Schema validation catches type errors (string vs number) but not semantic errors (date format, business-rule constraints). Implement: (1) Pydantic model validation (Python) or Zod (TypeScript) for structured validation before the function runs; (2) return a descriptive error as the function result (not an exception) — include the invalid value and what was expected; (3) the LLM typically self-corrects on the next call when given a clear error message. Retry budget: allow 2-3 self-correction attempts before surfacing the error to the user."
    }
  ],
  "sources": [
    { "label": "OpenAI Function Calling Documentation — official API reference", "url": "https://platform.openai.com/docs/guides/function-calling" },
    { "label": "Anthropic Tool Use Documentation — official API reference", "url": "https://docs.anthropic.com/en/docs/tool-use" }
  ]
}
