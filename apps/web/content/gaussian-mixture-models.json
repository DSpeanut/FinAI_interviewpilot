{
  "eli3": [
    "Imagine you're measuring the heights of a population that has two sub-populations — professional basketball players and general adults. Each group has a bell-curve distribution, but they overlap in the middle. A Gaussian Mixture Model says: the data was generated by K bell curves blended together. The algorithm figures out each bell curve's centre, width, and how much of the population it represents.",
    "Unlike K-Means, which makes hard assignments ('this point belongs to cluster 3'), GMM gives soft assignments: 'this point is 70% cluster 1 and 30% cluster 2.' This makes much more sense for points that sit near a boundary.",
    "The fitting algorithm is called Expectation-Maximisation (EM). It alternates between computing how likely each point belongs to each Gaussian (E-step) and re-estimating each Gaussian's parameters to best explain those assignments (M-step)."
  ],
  "whenWhy": [
    {
      "title": "Soft cluster memberships (probabilistic output)",
      "body": "When a data point sits between two clusters, hard assignment is arbitrary. GMM outputs P(cluster k | xᵢ) for every point — a proper probability. This is valuable in fraud detection (how suspicious is this transaction?), anomaly scoring, and any downstream decision that needs confidence estimates."
    },
    {
      "title": "Density estimation and generative modelling",
      "body": "GMM is a flexible density estimator: p(x) = Σₖ πₖ N(x; μₖ, Σₖ). Once fit, you can sample new data points from it, compute log-likelihoods for anomaly detection, or use it as a prior in Bayesian models."
    },
    {
      "title": "Elliptical clusters of different sizes and shapes",
      "body": "By allowing each component its own full covariance matrix Σₖ, GMM captures elongated, rotated elliptical clusters that K-Means cannot. Full covariance is the most flexible; diagonal covariance is more regularised (less data needed); spherical is equivalent to K-Means."
    },
    {
      "title": "Model selection via BIC/AIC",
      "body": "Unlike K-Means' heuristic elbow method, GMM supports principled model selection using BIC (Bayesian Information Criterion) or AIC. Fit models for K = 1, 2, …, Kmax and choose the K minimising BIC. This grounds cluster number selection in statistical theory."
    }
  ],
  "deepDiveIntro": [
    "A Gaussian Mixture Model is a weighted sum of K multivariate Gaussian distributions. The generative story is: to produce a data point, first sample a component k ~ Categorical(π₁, …, πₖ), then sample x ~ N(μₖ, Σₖ). Parameters θ = {πₖ, μₖ, Σₖ}ₖ₌₁ᴷ are estimated by maximising the observed log-likelihood via the EM algorithm.",
    "EM alternates between the E-step (compute soft responsibilities rᵢₖ = P(component k | xᵢ, θ)) and the M-step (update parameters to maximise the expected complete-data log-likelihood). Each iteration is guaranteed to increase the log-likelihood until convergence to a local maximum."
  ],
  "visualizationType": "gmm",
  "deepDive": [
    {
      "heading": "The GMM Model",
      "body": "The marginal density of x is a mixture of K Gaussians. πₖ are the mixing weights (Σπₖ = 1, πₖ ≥ 0). μₖ is the mean of component k. Σₖ is its covariance. This is a universal approximator of smooth densities: with enough components it can represent any continuous distribution. [1][2]",
      "formula": "p(x) = Σₖ₌₁ᴷ πₖ · N(x; μₖ, Σₖ)"
    },
    {
      "heading": "E-Step: Computing Responsibilities",
      "body": "Given current parameters, compute the posterior probability that point xᵢ was generated by component k using Bayes' theorem. rᵢₖ is called the responsibility of component k for point xᵢ. [1]",
      "formula": "rᵢₖ = πₖ N(xᵢ; μₖ, Σₖ) / Σⱼ πⱼ N(xᵢ; μⱼ, Σⱼ)"
    },
    {
      "heading": "M-Step: Updating Parameters",
      "body": "Re-estimate parameters using the soft assignments. Let Nₖ = Σᵢ rᵢₖ (effective number of points in component k). Update π̂ₖ = Nₖ/n, μ̂ₖ = (1/Nₖ)Σᵢ rᵢₖ xᵢ, Σ̂ₖ = (1/Nₖ)Σᵢ rᵢₖ (xᵢ−μ̂ₖ)(xᵢ−μ̂ₖ)ᵀ. These are weighted versions of the sample mean and covariance. [1]",
      "formula": "μ̂ₖ = (1/Nₖ) Σᵢ rᵢₖ xᵢ ,   Σ̂ₖ = (1/Nₖ) Σᵢ rᵢₖ (xᵢ−μ̂ₖ)(xᵢ−μ̂ₖ)ᵀ"
    },
    {
      "heading": "Covariance Types & Regularisation",
      "body": "Full Σₖ: most flexible, O(p²) parameters per component — needs much data. Diagonal Σₖ: assumes feature independence within each component, O(p) parameters — like Gaussian NB. Spherical: Σₖ = σ²I — equivalent to K-Means with soft assignments. Tied (shared Σ across components): analogous to LDA. Regularise by adding a small constant to the diagonal: Σₖ + εI to prevent singular matrices. [1]"
    },
    {
      "heading": "Model Selection: BIC & AIC",
      "body": "The log-likelihood increases monotonically with K, so use penalised criteria. BIC = −2 log L̂ + m log n, where m is the number of free parameters. AIC = −2 log L̂ + 2m. BIC penalises complexity more strongly and is consistent (selects true K as n → ∞ if true K is in the candidate set). Choose K minimising BIC. [1]",
      "formula": "BIC = −2 log L̂(θ̂) + m log n"
    }
  ],
  "youtubeLabel": "StatQuest: Gaussian Mixture Models",
  "youtubeUrl": "https://www.youtube.com/embed/REypj2sy_5U",
  "takeaways": [
    "GMM models data as a weighted sum of K Gaussians: p(x) = Σₖ πₖ N(x; μₖ, Σₖ). [1]",
    "Fitting uses EM: E-step computes soft responsibilities rᵢₖ, M-step updates π, μ, Σ using weighted statistics. [1]",
    "EM is guaranteed to increase log-likelihood at every step but converges to a local maximum — run multiple times with different initialisations. [1]",
    "Choose covariance type based on data size: full for large n, diagonal or spherical for small n or many components. [1]",
    "Use BIC to select K — it penalises model complexity and is consistent. [1]",
    "GMM is K-Means with soft assignments: K-Means ≈ GMM with spherical covariance and hard (0/1) responsibilities. [1][2]"
  ],
  "edgeCases": [
    {
      "q": "What is the singularity problem in GMM fitting?",
      "a": "If a component collapses to a single data point (μₖ = xᵢ for some i), its variance Σₖ → 0 and the likelihood → ∞. This is a degenerate solution. Fix: add a small regularisation constant to the diagonal of Σₖ (reg_covar in scikit-learn, default 1e-6), or use a Bayesian GMM with an informative prior on Σₖ. [1]"
    },
    {
      "q": "How does GMM differ from K-Means?",
      "a": "Three key differences: (1) Soft assignments — rᵢₖ ∈ [0,1] vs hard 0/1; (2) Flexible cluster shapes — each component has its own Σₖ allowing elliptical clusters; (3) Probabilistic interpretation — GMM is a generative model with a proper likelihood, enabling BIC model selection, density estimation, and sampling. K-Means is faster but less principled. [1][2]"
    },
    {
      "q": "How do you initialise EM for GMM?",
      "a": "Poor initialisation leads to bad local maxima. Common strategies: (1) run K-Means first and initialise μₖ with K-Means centroids; (2) random initialisation with multiple restarts (n_init in scikit-learn); (3) random assignment of data points to components, then compute weighted statistics. Scikit-learn uses K-Means initialisation by default. [1]"
    },
    {
      "q": "What is a Bayesian Gaussian Mixture Model?",
      "a": "A Bayesian GMM places Dirichlet priors on the mixing weights and Normal-Wishart priors on (μₖ, Σₖ). Inference via variational EM (BayesianGaussianMixture in scikit-learn) automatically prunes unused components by driving their πₖ → 0. This allows you to set K_max large and let the model find the effective K — a form of automatic model selection. [2]"
    },
    {
      "q": "Can GMM detect outliers?",
      "a": "Yes. The log-likelihood log p(x) under the fitted GMM is a principled anomaly score — low log-likelihood means the point is unlikely under any component. Alternatively, a point where max_k rᵢₖ is small (soft memberships are spread across all components) is likely an outlier. Threshold the log-likelihood at a percentile of the training distribution. [1]"
    }
  ],
  "sources": [
    { "label": "Bishop — Pattern Recognition and Machine Learning, Ch. 9.2", "url": "https://www.microsoft.com/en-us/research/publication/pattern-recognition-machine-learning/" },
    { "label": "Hastie, Tibshirani & Friedman — The Elements of Statistical Learning, Ch. 8.5", "url": "https://hastie.su.domains/ElemStatLearn/" }
  ]
}
