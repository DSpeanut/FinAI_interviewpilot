{
  "visualizationType": "short-term-memory-viz",
  "codeSnapshot": {
    "label": "short_term_memory.py",
    "code": "from langchain_openai import ChatOpenAI\nfrom langchain.memory import ConversationBufferWindowMemory, ConversationSummaryBufferMemory\nfrom langchain.chains import ConversationChain\n\nllm = ChatOpenAI(model=\"gpt-4o\", temperature=0)\n\n# --- Approach 1: Sliding Window Memory ---\n# Keeps only the last k=5 conversation turns in context.\n# Older messages are dropped entirely (hard truncation).\nwindow_memory = ConversationBufferWindowMemory(k=5, return_messages=True)\n\nwindow_chain = ConversationChain(\n    llm=llm,\n    memory=window_memory,\n    verbose=False,\n)\n\nwindow_chain.predict(input=\"My name is Alice and I work in fintech.\")\nwindow_chain.predict(input=\"What programming language do you recommend for data pipelines?\")\nwindow_chain.predict(input=\"Do you remember my name?\")\n# Only the last 5 messages are kept; early context may be lost as the\n# conversation grows beyond k turns.\n\nprint(\"Window memory buffer (last k turns):\")\nprint(window_memory.buffer_as_messages)\n\n\n# --- Approach 2: Summary-Buffer Memory ---\n# Keeps verbatim recent messages AND a running LLM-generated summary of\n# messages that have been pushed out by the token limit.  This preserves\n# semantic content even after older messages are compressed.\nsummary_memory = ConversationSummaryBufferMemory(\n    llm=llm,\n    max_token_limit=2000,   # When the buffer exceeds 2 000 tokens,\n                            # the oldest messages are summarised and\n                            # replaced with a compact digest.\n    return_messages=True,\n)\n\nsummary_chain = ConversationChain(\n    llm=llm,\n    memory=summary_memory,\n    verbose=False,\n)\n\nsummary_chain.predict(input=\"I'm building a RAG pipeline for legal documents.\")\nsummary_chain.predict(input=\"The documents are in PDF format, averaging 50 pages each.\")\nsummary_chain.predict(input=\"Which chunking strategy would you recommend?\")\n\n# When the buffer exceeds max_token_limit, LangChain automatically calls\n# the LLM to summarise the oldest messages.  The summary is stored in\n# summary_memory.moving_summary_buffer and prepended to the next prompt.\nprint(\"\\nSummary of compressed messages:\")\nprint(summary_memory.moving_summary_buffer)\nprint(\"\\nRecent verbatim messages:\")\nprint(summary_memory.buffer_as_messages)\n\n# --- Token counting with tiktoken ---\nimport tiktoken\n\nenc = tiktoken.encoding_for_model(\"gpt-4o\")\n\ndef count_tokens(messages: list[dict]) -> int:\n    \"\"\"Count tokens for a list of {role, content} messages.\"\"\"\n    total = 0\n    for msg in messages:\n        total += len(enc.encode(msg[\"content\"])) + 4  # 4 overhead per message\n    return total\n\n# Check budget before each LLM call:\nbudget = 128_000  # GPT-4o context window\nreserved_for_response = 2_000\nsystem_prompt_tokens = 500\n\ncurrent_history_tokens = count_tokens(\n    [{\"role\": m.type, \"content\": m.content}\n     for m in summary_memory.buffer_as_messages]\n)\navailable = budget - reserved_for_response - system_prompt_tokens - current_history_tokens\nprint(f\"\\nAvailable tokens for next user query + response: {available}\")\n"
  },
  "eli3": [
    "Short-term memory in AI agents is what the agent can 'hold in mind' right now — it's the current conversation history and recent observations that fit inside the LLM's context window. Like human working memory, it's fast to access but limited in size.",
    "Every time the agent runs, it reads from its short-term memory (the context window) to decide what to do. When the conversation gets too long, older messages fall off — like forgetting the beginning of a long conversation when you're focused on the current topic.",
    "The context window IS the short-term memory. Bigger context windows (GPT-4's 128k tokens, Claude's 200k tokens) give agents a larger working memory, but it's still finite. Managing what goes into the context window is one of the most important engineering challenges in building long-running agents."
  ],
  "whenWhy": [
    {
      "title": "Maintaining conversation continuity within a session",
      "body": "Short-term memory enables multi-turn conversations: the agent remembers what was said earlier in the same session, refers back to prior context, and builds coherent responses. Without it, every user message would be treated as a fresh query with no context. The entire message history (user + assistant turns) is the short-term memory. For short conversations this is sufficient; for long conversations or multi-tool agent loops, active management is needed."
    },
    {
      "title": "Holding tool outputs for multi-step reasoning",
      "body": "In the agent loop, each tool output (search result, code output, API response) is appended to the context. The agent reasons over the accumulated observations. Short-term memory is what makes multi-step reasoning possible — the agent can refer to a result from Step 1 when deciding what to do in Step 5. Without a context window holding all intermediate results, each step would be isolated and the agent couldn't synthesise across them."
    },
    {
      "title": "Zero-persistence working space",
      "body": "Short-term memory is ephemeral by design: it exists only for the duration of a single session or agent run. This makes it appropriate for temporary state that doesn't need to persist across sessions: draft calculations, intermediate search results, working hypotheses. The ephemeral nature also means no privacy/storage concerns — once the session ends, the memory is gone. Contrast with long-term memory, which requires explicit storage and retrieval infrastructure."
    }
  ],
  "deepDiveIntro": [
    "In LLM-based agents, the context window is the physical substrate of short-term memory: every token the model attends over during a forward pass constitutes its entire working memory. All reasoning, tool output accumulation, and multi-turn conversation history must fit within this fixed-size window. Capacity is measured in tokens — GPT-4 Turbo supports 128k tokens (~96k words), Claude 3.5 Sonnet 200k tokens (~150k words), and Gemini 1.5 Pro up to 1M tokens. Despite large windows, performance degrades when relevant content is positioned in the middle of very long contexts — the 'lost in the middle' phenomenon (Liu et al. 2023) — making raw window size an incomplete solution to the short-term memory problem.",
    "Active token budget management is therefore a first-class engineering concern. A practical 128k-token budget is consumed by: system prompt (1–3k), tool definitions (~1–2k per tool), accumulated conversation history (grows with every turn), tool outputs (can be large), and reserved response space (~2k). In a 10-turn session with 5 tool calls returning verbose payloads, 40–80k tokens can be consumed before any summarisation is applied. The principal management strategies are: (1) summarisation — replace older turns with a compressed digest; (2) sliding window — retain only the last N messages; (3) selective retention — keep only the highest-priority observations; and (4) output compression — truncate or summarise verbose tool responses before injecting them. The goal is to maximise information density within the available budget."
  ],
  "deepDive": [
    {
      "heading": "Context Window as RAM: The Token Budget",
      "body": "Practical token budgeting for a 128k-token context: System prompt (1-3k tokens), conversation history (variable, grows per turn), tool definitions (0.5-2k per tool, multiplied by number of tools), tool outputs (variable, can be large), and the response (reserve ~2k). A 10-turn conversation with 5 tool calls can easily consume 20-40k tokens. With 3 tool definitions (1.5k) and 10 large search results (5k each = 50k), the context fills quickly. Budget allocation strategy: system prompt is fixed, response is fixed, tool definitions scale with tools exposed — the dynamic budget is primarily conversation history + tool outputs. [1]"
    },
    {
      "heading": "Summarisation-Based Memory Compression",
      "body": "When the context approaches its limit: trigger a summarisation step — call the LLM to summarise the conversation/observations so far into a dense paragraph; replace the summarised messages with the summary; continue with the freed-up token budget. Implementation: track running token count; when > 70% of the limit, summarise the oldest 50% of messages; insert the summary as a special 'memory' message. Risk: summaries lose detail — if a specific fact in the summarised portion is needed later, it may be missing from the summary. Use structured summaries (bullet points of key facts, decisions, and open questions) over prose summaries to preserve retrievable detail. [1]"
    },
    {
      "heading": "Lost-in-the-Middle Degradation",
      "body": "Liu et al. (2023) showed that LLM performance on multi-document QA degrades significantly when the relevant document is in the middle of a long context — models attend much more strongly to the beginning and end of the context. For short-term memory management: put the most recently observed and most critical information at the end of the context (recency bias of LLMs) and keep the system prompt / task description at the beginning. Avoid burying critical facts in the middle of long tool outputs. Truncate tool outputs to the most relevant sections rather than injecting full, raw outputs. [2]"
    },
    {
      "heading": "Sliding Window and Summarisation Strategies",
      "body": "The two dominant approaches to bounded-context management are sliding window and hierarchical summarisation, each with distinct trade-offs. A sliding window drops the oldest N messages when the token budget is exceeded — simple to implement but causes hard information loss: any fact established early in the conversation is silently discarded. Hierarchical summarisation addresses this by replacing dropped messages with a structured digest before removing them: a prompt extracts key decisions, facts, and open questions into a compact representation (typically 100–300 tokens), preserving semantic content at the cost of verbatim fidelity. Hybrid strategies combine both: maintain a rolling summary of all sessions older than a threshold and a verbatim sliding window for the most recent K turns. This mirrors human episodic memory — detailed for recent events, gist-level for older ones. The critical engineering choice is the trigger threshold (70–80% of the token budget is typical) and the summary format (structured JSON bullets outperform prose for downstream retrieval). [1][2]"
    },
    {
      "heading": "Token Budget Management in Production",
      "body": "Every LLM call against a finite context window requires explicit token accounting before the request is sent. The practical approach uses tiktoken (OpenAI's tokeniser library) to count tokens at each turn: encode each message, add the per-message overhead (4 tokens for the role/separator markup in chat models), and compare the running total against the context limit minus two reserved zones — space for the system prompt (typically 500–3 000 tokens) and space for the expected response (1 500–4 000 tokens depending on output verbosity). When the budget for history is exhausted, truncation or summarisation must fire before the API call is made, not after. In production, tracking token usage per request exposes critical signals: average session length in tokens, frequency of context overflow events, and the proportion of the window consumed by tool outputs versus conversation. These metrics inform decisions such as adjusting the sliding window size, compressing tool outputs more aggressively, or raising the summarisation trigger threshold. Monitoring across sessions also surfaces model-specific degradation: if token utilisation climbs steadily over sessions without forgetting firing, it indicates that the forgetting mechanism is not engaging, which leads to out-of-context errors or silent truncation at the API layer. [1]"
    },
    {
      "heading": "Conversation Summarization Strategies",
      "body": "When messages must leave the context window, the summarisation strategy chosen determines how much semantic fidelity is preserved. Three dominant approaches exist: (1) Progressive summarisation — each time the buffer overflows, the LLM summarises only the oldest N messages and appends that digest to a running summary. The running summary therefore grows incrementally, capturing cumulative context without reprocessing the entire history each time. LangChain's ConversationSummaryBufferMemory implements this pattern. The quality risk is error accumulation: each summarisation pass may omit details that become relevant many turns later. (2) Rolling window (hard drop) — the simplest approach: keep the last K turns verbatim and discard everything older. Zero compression overhead, but all information outside the window is irrecoverably lost. Appropriate when earlier turns genuinely have no bearing on the current task (e.g., tool results from completed sub-tasks). (3) Hierarchical summarisation — maintain multiple layers: verbatim for the most recent K turns, a structured summary of turns K+1 through 3K, and an abstract digest of everything older. Each layer is progressively more compressed. This most closely mirrors human episodic memory and preserves the most information, but is the most expensive to maintain: three summarisation passes may be required when the hierarchy rolls. The right strategy depends on task type: for open-ended conversations where any past turn might resurface as relevant, progressive or hierarchical summarisation is essential; for single-task tool-calling loops where earlier intermediate results are disposable after use, a rolling window suffices. [1][2]"
    }
  ],
  "takeaways": [
    "Short-term memory = context window: all tokens the LLM currently attends over, limited by window size. [1]",
    "Grows with each turn; requires active management (summarisation, sliding window) for long-running agents. [1]",
    "Token budget: allocate for system prompt (fixed), tools (fixed), history (dynamic), responses (reserved). [1]",
    "LLMs attend more to beginning/end of context — place critical info at start or end, not middle. [2]",
    "Short-term memory is ephemeral; for persistence across sessions, use long-term memory systems. [1]"
  ],
  "edgeCases": [
    {
      "q": "How do you decide what to keep vs. discard when the context is full?",
      "a": "Priority hierarchy for context retention: (1) System prompt — never discard, always at top; (2) Current task/query — always keep the most recent user request; (3) Recent observations — the last 3-5 tool results, as they're most likely needed for immediate next steps; (4) Key facts extracted — if a critical fact was established early (a person's name, a company's revenue), keep it even if the surrounding conversation is summarised; (5) Earlier conversation history — least valuable, first to summarise. Implement with a token-aware message list: track cumulative tokens, and when a budget threshold is hit, compress/drop messages in reverse priority order. Structured summaries over full message retention."
    },
    {
      "q": "What's the performance impact of using very large context windows (1M tokens) instead of active memory management?",
      "a": "Very large contexts reduce the need for explicit memory management but introduce costs: (1) Latency — attention is O(n²) in sequence length; a 1M token context is ~10,000x slower to compute than a 10k context (though sparse attention implementations mitigate this); (2) Cost — API pricing scales linearly with input tokens; 1M tokens per call is expensive; (3) Quality degradation — the lost-in-the-middle effect worsens with longer contexts; relevant information can be 'drowned out' by irrelevant content even if technically within the window; (4) No forced prioritisation — active memory management forces you to think about what's important; unlimited context allows lazy accumulation of irrelevant content. Large contexts are useful for one-time document analysis; active management is better for long-running multi-session agents."
    }
  ],
  "sources": [
    { "label": "Liu et al. (2023) — Lost in the Middle: How Language Models Use Long Contexts", "url": "https://arxiv.org/abs/2307.03172" },
    { "label": "Wang et al. (2024) — A Survey on Large Language Model based Autonomous Agents", "url": "https://arxiv.org/abs/2308.11432" }
  ]
}
